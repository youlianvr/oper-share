// Dashi PPT export engine — proprietary, licensed for use only as part of the Dashi PPT skill. See LICENSE.
import{mkdirSync,writeFileSync}from"node:fs";import{createRequire}from"node:module";import path from"node:path";import{PNG}from"pngjs";var STYLE_KEYS=["display","visibility","opacity","content","position","left","top","right","bottom","width","height","zIndex","backgroundColor","backgroundImage","backgroundClip","webkitBackgroundClip","backgroundSize","backgroundPosition","borderTopWidth","borderRightWidth","borderBottomWidth","borderLeftWidth","borderTopColor","borderRightColor","borderBottomColor","borderLeftColor","borderTopStyle","borderRightStyle","borderBottomStyle","borderLeftStyle","borderTopLeftRadius","borderTopRightRadius","borderBottomRightRadius","borderBottomLeftRadius","boxShadow","color","fill","webkitTextFillColor","fontFamily","fontSize","fontWeight","fontStyle","lineHeight","letterSpacing","textAlign","textDecorationLine","textTransform","webkitTextStrokeColor","webkitTextStrokeWidth","whiteSpace","writingMode","textOrientation","verticalAlign","objectFit","objectPosition","transform","filter","clipPath","overflow","mask","maskImage","webkitMask","webkitMaskImage","mixBlendMode"];async function collectActiveSlide(slideNumber){let slide=document.querySelector("#deck > .slide.active, #deck > .slide[data-deck-active]");if(!slide)return{index:slideNumber,rect:{x:0,y:0,w:1920,h:1080},root:null,warnings:[{type:"missing-slide"}],summary:null};let rawRect=slide.getBoundingClientRect(),slideRect=rectObject(rawRect),warnings=[];markUnicornOverlayText(slide,slideRect);let root=await captureElement(slide,slideRect,warnings,0,slideNumber,slideClipRect(slideRect)),summary=summarizeCapturedTree(root);return summary.key=slide.dataset.vmVariantStateId||slide.dataset.vmSlideId||slide.dataset.layoutKey||slide.id||"",summary.sourceSlideId=slide.dataset.vmSourceSlideId||slide.dataset.vmSlideId||slide.id||"",summary.variantStateId=slide.dataset.vmVariantStateId||"",summary.variantKind=slide.dataset.vmVariantKind||"",{index:slideNumber,rect:slideRect,root,warnings,summary}}async function captureElement(el,slideRect,warnings,depth,slideIndex,clipRect=null){if(!(el instanceof Element)||isMediaChrome(el))return null;let style=styleWithCumulativeRotation(readStyle(el),el);if(!isVisibleElement(el,slideRect,style))return null;let activeClip=clipRect||slideClipRect(slideRect),rawRect=el.getBoundingClientRect(),clipped=intersectClientRect(rawRect,activeClip);if(!clipped)return null;let tag=el.tagName.toLowerCase(),node={tag,slideIndex,rect:rectObject(clipped),style,children:[]},renderRect=elementRenderRect(el,clipped,style,slideRect);renderRect&&(node.renderRect=renderRect),tag==="a"&&el.href&&!String(el.getAttribute("href")||"").startsWith("#")&&(node.href=el.href);let childClip=nextChildClipRect(el,style,rawRect,activeClip);if(el.classList?.contains("bt-unicorn-frame")){let exportId=`editable-pptx-${slideIndex}-${depth}-${Math.random().toString(36).slice(2,9)}`;el.setAttribute("data-editable-pptx-export-id",exportId),el.setAttribute("data-editable-pptx-material-background",""),node.exportId=exportId,node.elementScreenshot=!0,node.imageKind="unicorn-background";let textNodes=collectDomFallbackTextNodes(el,slideRect,slideIndex),overlayText=visibleTextInScreenshotRect(el,slideRect),overlayPaint=visibleOverlayPaintInScreenshotRect(el,slideRect);textNodes.length&&node.children.push(...textNodes),(textNodes.length||overlayText.count)&&(node.stripTextForScreenshot=!0),overlayPaint.count&&(node.stripOverlayForScreenshot=!0);let risk=fallbackTextRisk(el,slideRect);return overlayText.count?warnings.push({slide:slideIndex,type:"node-image-fallback-text-extracted",node:"unicorn-background",textCount:overlayText.count,sample:overlayText.sample,scope:"screenshot-rect"}):risk.count&&textNodes.length?warnings.push({slide:slideIndex,type:"node-image-fallback-text-extracted",node:"unicorn-background",textCount:textNodes.length,sample:risk.sample,scope:"descendant"}):risk.count&&warnings.push({slide:slideIndex,type:"node-image-fallback-text-risk",node:"unicorn-background",textCount:risk.count,sample:risk.sample}),overlayPaint.count&&warnings.push({slide:slideIndex,type:"node-image-fallback-overlay-extracted",node:"unicorn-background",overlayCount:overlayPaint.count,sample:overlayPaint.sample,scope:"screenshot-rect"}),warnings.push({slide:slideIndex,type:"node-image-fallback",node:"unicorn-background",count:1}),node}if(tag==="img")return node.imageData=await elementImageData(el,el.currentSrc||el.src||el.getAttribute("src")||""),node.imageKind="img",node.imageData||warnings.push({slide:slideIndex,type:"image-skipped",reason:"unreadable-img"}),node;if(tag==="canvas"){try{node.imageData=el.toDataURL("image/png"),node.imageKind="canvas",warnings.push({slide:slideIndex,type:"node-image-fallback",node:"canvas",count:1})}catch{warnings.push({slide:slideIndex,type:"canvas-skipped",reason:"tainted-or-empty"})}return node}if(tag==="svg"){let svgTexts=collectSvgTextNodes(el,slideRect,slideIndex);node.imageData=await svgElementData(el,clipped.width,clipped.height,{stripText:svgTexts.length>0}),node.imageKind="svg";let risk=svgTextRisk(el);return risk.count&&svgTexts.length?warnings.push({slide:slideIndex,type:"node-image-fallback-text-extracted",node:"svg",textCount:svgTexts.length,sample:risk.sample}):risk.count&&warnings.push({slide:slideIndex,type:"node-image-fallback-text-risk",node:"svg",textCount:risk.count,sample:risk.sample}),node.imageData?warnings.push({slide:slideIndex,type:"node-image-fallback",node:"svg",count:1}):warnings.push({slide:slideIndex,type:"svg-skipped",reason:"rasterize-failed"}),node.children.push(...svgTexts),node}if(shouldScreenshotImageSlot(el)){let exportId=`editable-pptx-${slideIndex}-${depth}-${Math.random().toString(36).slice(2,9)}`,screenshotRect=visualScreenshotRect(rawRect,style,slideRect);return el.setAttribute("data-editable-pptx-export-id",exportId),node.exportId=exportId,node.elementScreenshot=!0,node.imageKind="masked-element",node.rect=rectObject(screenshotRect),node.screenshotRect=rectObject(screenshotRect),node.screenshotMode="screenshot-rect",warnings.push({slide:slideIndex,type:"node-image-fallback",node:"image-slot",count:1,source:"browser-image-slot"}),node}let visualFallback=visualScreenshotFallbackKind(el,style,clipped,rawRect,slideRect);if(visualFallback){let exportId=`editable-pptx-${slideIndex}-${depth}-${Math.random().toString(36).slice(2,9)}`,screenshotRect=visualScreenshotRect(rawRect,style,slideRect);el.setAttribute("data-editable-pptx-export-id",exportId),node.exportId=exportId,node.elementScreenshot=!0,node.imageKind=visualFallback,node.rect=rectObject(screenshotRect),node.screenshotRect=rectObject(screenshotRect),node.screenshotMode="screenshot-rect";let textNodes=collectDomFallbackTextNodes(el,slideRect,slideIndex),overlayText=visibleTextInScreenshotRect(el,slideRect,screenshotRect),overlayPaint=visibleOverlayPaintInScreenshotRect(el,slideRect,screenshotRect);return textNodes.length&&node.children.push(...textNodes),node.stripTextForScreenshot=textNodes.length>0||overlayText.count>0,node.stripOverlayForScreenshot=overlayPaint.count>0,(textNodes.length||overlayText.count)&&warnings.push({slide:slideIndex,type:"node-image-fallback-text-extracted",node:visualFallback,textCount:Math.max(textNodes.length,overlayText.count),sample:overlayText.sample}),overlayPaint.count&&warnings.push({slide:slideIndex,type:"node-image-fallback-overlay-extracted",node:visualFallback,overlayCount:overlayPaint.count,sample:overlayPaint.sample,scope:"screenshot-rect"}),warnings.push({slide:slideIndex,type:"node-image-fallback",node:visualFallback,count:1,source:"browser-visual-effect"}),node}if(shouldUseLocalMaterialFallback(el,style,clipped,slideRect)){let exportId=`editable-pptx-${slideIndex}-${depth}-${Math.random().toString(36).slice(2,9)}`,screenshotRect=visualScreenshotRect(rawRect,style,slideRect);el.setAttribute("data-editable-pptx-export-id",exportId),el.setAttribute("data-editable-pptx-material-background",""),node.exportId=exportId,node.elementScreenshot=!0,node.imageKind="material-background",node.rect=rectObject(screenshotRect),node.screenshotRect=rectObject(screenshotRect),node.screenshotMode="screenshot-rect";let materialText=fallbackTextRisk(el,slideRect),overlayText=visibleTextInScreenshotRect(el,slideRect,screenshotRect),overlayPaint=visibleOverlayPaintInScreenshotRect(el,slideRect,screenshotRect);node.stripTextForScreenshot=materialText.count>0||overlayText.count>0,node.stripOverlayForScreenshot=overlayPaint.count>0,(materialText.count||overlayText.count)&&warnings.push({slide:slideIndex,type:"node-image-fallback-text-extracted",node:"material-background",textCount:Math.max(materialText.count,overlayText.count),sample:materialText.sample||overlayText.sample}),overlayPaint.count&&warnings.push({slide:slideIndex,type:"node-image-fallback-overlay-extracted",node:"material-background",overlayCount:overlayPaint.count,sample:overlayPaint.sample,scope:"screenshot-rect"}),warnings.push({slide:slideIndex,type:"node-image-fallback",node:"material-background",count:1,source:"browser-local-material"})}let bg=backgroundUrl(style.backgroundImage);if(node.imageKind!=="material-background")if(bg){let isTurbulence=isTurbulenceDataImage(bg);node.backgroundImageData=await fetchImageDataUrl(bg,clipped.width,clipped.height,isTurbulence?.02:1),isTurbulence&&(node.backgroundImageTransparency=0),node.backgroundImageData||warnings.push({slide:slideIndex,type:"background-image-skipped",url:bg.slice(0,160)})}else String(style.backgroundImage||"").includes("repeating-linear-gradient")?(node.patternImageData=patternBackgroundImageData(style.backgroundImage,clipped.width,clipped.height,maxCssRadius(style,clipped.width,clipped.height)),node.patternImageData&&warnings.push({slide:slideIndex,type:"node-image-fallback",node:"css-pattern-background",count:1})):!isTextClippedBackground(style)&&String(style.backgroundImage||"").includes("gradient")&&!shouldSkipDecorativeGradientFallback(el,style,clipped,slideRect)&&!shouldUseNativeGradientShape(style,clipped.width,clipped.height)&&!shouldUseNativeGradientShape(style,(el.offsetWidth||clipped.width)*(slideRect.w||1920)/1920,(el.offsetHeight||clipped.height)*(slideRect.h||1080)/1080)&&!String(style.clipPath||"").includes("polygon(")&&(node.backgroundImageData=gradientBackgroundImageData(style.backgroundImage,rawRect.width||clipped.width,rawRect.height||clipped.height,maxCssRadius(style,rawRect.width||clipped.width,rawRect.height||clipped.height),backgroundCropForClippedRect(rawRect,clipped)),node.backgroundImageData&&warnings.push({slide:slideIndex,type:"node-image-fallback",node:"css-gradient-background",count:1}));let before=node.imageKind==="material-background"?null:capturePseudoElement(el,"::before",slideRect,slideIndex,childClip);before&&node.children.push(before);let wholeText=captureWholeTextElement(el,slideRect,style,slideIndex,childClip);if(wholeText){node.children.push(wholeText);let after2=node.imageKind==="material-background"?null:capturePseudoElement(el,"::after",slideRect,slideIndex,childClip);return after2&&node.children.push(after2),node}let childNodeLists=[el.childNodes];el.shadowRoot&&childNodeLists.push(el.shadowRoot.childNodes);for(let childNodes of childNodeLists)for(let child of childNodes)if(child.nodeType===Node.TEXT_NODE){if(isDuplicatedDecoratorTextNode(child))continue;let textNode=captureTextNode(child,el,slideRect,style,slideIndex,childClip);Array.isArray(textNode)?node.children.push(...textNode):textNode&&node.children.push(textNode)}else if(child.nodeType===Node.ELEMENT_NODE){let childNode=await captureElement(child,slideRect,warnings,depth+1,slideIndex,childClip);childNode&&node.children.push(childNode)}let after=node.imageKind==="material-background"?null:capturePseudoElement(el,"::after",slideRect,slideIndex,childClip);return after&&node.children.push(after),node}function capturePseudoElement(el,pseudo,slideRect,slideIndex,clipRect=null){let style=readStyle(el,pseudo),content=String(style.content||"").trim(),hasText=content&&content!=="none"&&content!=="normal"&&content!=='""'&&content!=="''",hasVisual=hasPaint(style.backgroundColor)||backgroundUrl(style.backgroundImage)||String(style.backgroundImage||"").includes("gradient")||hasAnyBorder(style);if(style.display==="none"||style.visibility==="hidden"||!hasText&&!hasVisual)return null;let rect=pseudoRect(el,style,slideRect),clipped=rect?intersectClientRect(rect,clipRect||slideClipRect(slideRect)):null;return!clipped||clipped.width<1||clipped.height<1?null:{tag:"pseudo",slideIndex,rect:rectObject(clipped),style,clipped:!sameClientRect(rect,clipped),text:hasText?content.replace(/^['"]|['"]$/g,""):"",children:[]}}function pseudoRect(el,style,slideRect){let parent=el.getBoundingClientRect(),stageScaleX=(slideRect?.w||1920)/1920,stageScaleY=(slideRect?.h||1080)/1080,parentWidth=el.offsetWidth||parent.width/stageScaleX,parentHeight=el.offsetHeight||parent.height/stageScaleY,width=cssLengthPx(style.width,parentWidth),height=cssLengthPx(style.height,parentHeight);if(width==null||height==null)return null;let borderLeft=parseFloat(style.borderLeftWidth||"0")||0,borderRight=parseFloat(style.borderRightWidth||"0")||0,borderTop=parseFloat(style.borderTopWidth||"0")||0,borderBottom=parseFloat(style.borderBottomWidth||"0")||0,boxWidth=width+borderLeft+borderRight,boxHeight=height+borderTop+borderBottom,left=cssLengthPx(style.left,parentWidth),top=cssLengthPx(style.top,parentHeight),right=cssLengthPx(style.right,parentWidth),bottom=cssLengthPx(style.bottom,parentHeight),parentStyle=getComputedStyle(el),parentRotation=rotateFromTransform(parentStyle.transform);if(parentRotation&&(left!=null||right!=null)&&(top!=null||bottom!=null)){let[originXRaw="0",originYRaw="0"]=String(parentStyle.transformOrigin||"").split(/\s+/),originX=cssLengthPx(originXRaw,parentWidth)??parentWidth/2,originY=cssLengthPx(originYRaw,parentHeight)??parentHeight/2,localX=left??parentWidth-(right+boxWidth),localY=top??parentHeight-(bottom+boxHeight),translate2=translateFromTransform(style.transform,boxWidth,boxHeight),dx=(localX+translate2.x+boxWidth/2-originX)*stageScaleX,dy=(localY+translate2.y+boxHeight/2-originY)*stageScaleY,angle=parentRotation*Math.PI/180,cx=parent.left+parent.width/2+dx*Math.cos(angle)-dy*Math.sin(angle),cy=parent.top+parent.height/2+dx*Math.sin(angle)+dy*Math.cos(angle);return{x:cx-boxWidth*stageScaleX/2,y:cy-boxHeight*stageScaleY/2,w:boxWidth*stageScaleX,h:boxHeight*stageScaleY}}let translate=translateFromTransform(style.transform,boxWidth,boxHeight),x=(left!=null?parent.left+left*stageScaleX:right!=null?parent.right-(right+boxWidth)*stageScaleX:parent.left)+translate.x*stageScaleX,y=(top!=null?parent.top+top*stageScaleY:bottom!=null?parent.bottom-(bottom+boxHeight)*stageScaleY:parent.top)+translate.y*stageScaleY;return{x,y,w:boxWidth*stageScaleX,h:boxHeight*stageScaleY}}function isDuplicatedDecoratorTextNode(textNode){let value=normalizeText(textNode?.textContent||"");if(!value||Array.from(value).length>2||/[\p{L}\p{N}]/u.test(value))return!1;let next=textNode.nextSibling;for(;next&&next.nodeType===Node.TEXT_NODE&&!normalizeText(next.textContent||"");)next=next.nextSibling;if(!next||next.nodeType!==Node.TEXT_NODE)return!1;let nextValue=normalizeText(next.textContent||"");if(!nextValue.startsWith(value))return!1;let rest=nextValue.slice(value.length);return!rest||/^\s/.test(rest)}function captureWholeTextElement(el,slideRect,style,slideIndex,clipRect=null){let tag=el.tagName.toLowerCase();if(!["p","li","blockquote"].includes(tag))return null;let inlineChildren=[...el.children];if(!inlineChildren.length||!inlineChildren.every(isInlineTextChild)||inlineChildren.some(hasInlineVisualTreatment))return null;let value=normalizeText(el.innerText||el.textContent||"");if(value.length<18)return null;let range=document.createRange();range.selectNodeContents(el);let lineRects=[...range.getClientRects()].filter(rect=>rect.width>1&&rect.height>1);range.detach?.();let rawRect=el.getBoundingClientRect(),clipped=intersectClientRect(rawRect,clipRect||slideClipRect(slideRect));return!clipped||clipped.width<1||clipped.height<1?null:{tag:"#text",slideIndex,rect:rectObject(clipped),style:effectiveTextStyle(el,slideRect),text:value,textMetrics:{usesLineBox:!0},singleLine:lineRects.length<=1&&!/[\r\n]/.test(value),clipped:!sameClientRect(rawRect,clipped),parentTag:tag,children:[]}}function isInlineTextChild(child){let display=getComputedStyle(child).display;return display==="inline"||display==="inline-block"||display==="contents"}function hasInlineVisualTreatment(child){let style=getComputedStyle(child);return hasPaint(style.backgroundColor)||String(style.backgroundImage||"").includes("gradient")||hasAnyBorder(style)||parseFloat(style.borderTopLeftRadius||"0")>0||parseFloat(style.borderTopRightRadius||"0")>0||parseFloat(style.borderBottomRightRadius||"0")>0||parseFloat(style.borderBottomLeftRadius||"0")>0||style.boxShadow&&style.boxShadow!=="none"}function captureTextNode(textNode,parent,slideRect,style,slideIndex,clipRect=null){let keepWhitespace=["pre","pre-wrap","pre-line","break-spaces"].includes(style.whiteSpace),value=keepWhitespace?textNode.textContent||"":normalizeText(textNode.textContent||"");if(!value.trim())return null;let range=document.createRange();range.selectNodeContents(textNode);let lineRects=[...range.getClientRects()].filter(rect=>rect.width>1&&rect.height>1),singleLine=lineRects.length<=1&&!/[\r\n]/.test(value),tag=parent.tagName.toLowerCase(),wrappedFragments=splitWrappedTextNode(textNode,lineRects,slideRect,keepWhitespace,clipRect);if(wrappedFragments.length>1){range.detach?.();let parentStyle2=effectiveTextStyle(parent,slideRect);return wrappedFragments.map(fragment=>({tag:"#text",slideIndex,rect:fragment.rect,style:parentStyle2,text:fragment.text,singleLine:!0,clipped:fragment.clipped,parentTag:tag,requiredText:parent.hasAttribute("data-editable-pptx-required-text"),href:parent.closest("a")?.href||void 0,children:[]}))}let bounds=range.getBoundingClientRect(),rawTextRect=lineRects.length===1?lineRects[0]:bounds;range.detach?.();let textNodeCount=[...parent.childNodes].filter(child=>child.nodeType===Node.TEXT_NODE&&normalizeText(child.textContent||"")).length,visibleElementChildren2=[...parent.children].filter(child=>isVisibleElement(child,slideRect)).length,textMetrics=null;if(singleLine&&textNodeCount===1&&visibleElementChildren2===0&&hasInlineVisualTreatment(parent)){let parentRect=parent.getBoundingClientRect();intersectClientRect(parentRect,clipRect||slideClipRect(slideRect))&&parentRect.height>rawTextRect.height+2&&parentRect.width>rawTextRect.width+2&&(rawTextRect={left:rawTextRect.left,top:parentRect.top,right:rawTextRect.right,bottom:parentRect.bottom,width:rawTextRect.width,height:parentRect.height},textMetrics={verticalContainer:!0})}let clipped=intersectClientRect(rawTextRect,clipRect||slideClipRect(slideRect));if(["h1","h2","h3","h4","h5","h6","p","li","td","th","blockquote","button","label"].includes(tag)&&textNodeCount===1&&visibleElementChildren2===0&&(rawTextRect=parent.getBoundingClientRect(),clipped=intersectClientRect(rawTextRect,clipRect||slideClipRect(slideRect))||clipped),!clipped||clipped.width<1||clipped.height<1)return null;let parentStyle=effectiveTextStyle(parent,slideRect),isClipped=!sameClientRect(rawTextRect,clipped);return{tag:"#text",slideIndex,rect:rectObject(clipped),style:parentStyle,text:value,textMetrics,singleLine,clipped:isClipped,lineRect:singleLine&&isClipped?rectObject(rawTextRect):void 0,parentTag:tag,requiredText:parent.hasAttribute("data-editable-pptx-required-text"),href:parent.closest("a")?.href||void 0,children:[]}}function splitWrappedTextNode(textNode,lineRects,slideRect,keepWhitespace,clipRect=null){if(lineRects.length<=1)return[];let raw=textNode.textContent||"";if(!raw.trim()||raw.length>600)return[];let lines=groupLineRects(lineRects);if(lines.length<=1||lines.length>20)return[];let chars=Array.from(raw),offsets=[],offset=0;for(let char of chars){let end=offset+char.length;offsets.push({char,start:offset,end}),offset=end}let spans=lines.map(()=>({start:1/0,end:-1/0}));for(let item of offsets){let rect=rangeBoundsForOffsets(textNode,item.start,item.end);if(!rect||rect.width<=.25||rect.height<=.25)continue;let lineIndex=lineIndexForRect(rect,lines);lineIndex<0||(spans[lineIndex].start=Math.min(spans[lineIndex].start,item.start),spans[lineIndex].end=Math.max(spans[lineIndex].end,item.end))}let fragments=[];for(let span of spans){if(!Number.isFinite(span.start)||span.end<=span.start)continue;let text=keepWhitespace?raw.slice(span.start,span.end):normalizeText(raw.slice(span.start,span.end));if(!text.trim())continue;let rect=rangeBoundsForOffsets(textNode,span.start,span.end),clipped=rect?intersectClientRect(rect,clipRect||slideClipRect(slideRect)):null;!clipped||clipped.width<1||clipped.height<1||fragments.push({text,rect:rectObject(clipped),clipped:!sameClientRect(rect,clipped)})}return fragments.length>1?fragments:[]}function groupLineRects(rects){let lines=[];for(let rect of[...rects].sort((a,b)=>a.top-b.top||a.left-b.left)){let centerY=rect.top+rect.height/2,line=lines.find(item=>Math.min(item.bottom,rect.bottom)-Math.max(item.top,rect.top)>Math.min(item.height,rect.height)*.5||Math.abs(centerY-item.centerY)<=Math.max(2,Math.min(item.height,rect.height)*.35));if(!line){lines.push({left:rect.left,right:rect.right,top:rect.top,bottom:rect.bottom,width:rect.width,height:rect.height,centerY});continue}line.left=Math.min(line.left,rect.left),line.right=Math.max(line.right,rect.right),line.top=Math.min(line.top,rect.top),line.bottom=Math.max(line.bottom,rect.bottom),line.width=line.right-line.left,line.height=line.bottom-line.top,line.centerY=line.top+line.height/2}return lines.sort((a,b)=>a.top-b.top||a.left-b.left)}function lineIndexForRect(rect,lines){let best=-1,bestScore=1/0,centerY=rect.top+rect.height/2;for(let index=0;index<lines.length;index+=1){let line=lines[index],overlap=Math.max(0,Math.min(line.bottom,rect.bottom)-Math.max(line.top,rect.top)),score=Math.abs(centerY-line.centerY)-overlap;score<bestScore&&(best=index,bestScore=score)}return best}function rangeBoundsForOffsets(textNode,start,end){if(end<=start)return null;let range=document.createRange();try{range.setStart(textNode,start),range.setEnd(textNode,end);let rects=[...range.getClientRects()].filter(rect=>rect.width>.25&&rect.height>.25);return rects.length&&rects.length===1?rects[0]:range.getBoundingClientRect()}finally{range.detach?.()}}function effectiveTextStyle(parent,slideRect){let style=styleWithCumulativeRotation(readStyle(parent),parent);if(parent.closest?.("[data-editable-pptx-material-background]")&&(style.materialBackground="true"),!transparentCssPaint(style.webkitTextFillColor)||!transparentCssPaint(style.color)||isTextClippedBackground(style)||hasTextPaintSource(style))return style;let slide=document.querySelector("#deck > .slide.active, #deck > .slide[data-deck-active]");for(let el=parent.parentElement;el&&el!==slide?.parentElement;el=el.parentElement){if(!isVisibleElement(el,slideRect))continue;let ancestor=readStyle(el);if(!(!isTextClippedBackground(ancestor)&&!hasTextPaintSource(ancestor)))return{...style,color:transparentCssPaint(style.color)?ancestor.color:style.color,fill:transparentCssPaint(style.fill)?ancestor.fill:style.fill,webkitTextFillColor:transparentCssPaint(style.webkitTextFillColor)?ancestor.webkitTextFillColor:style.webkitTextFillColor,backgroundImage:isTextClippedBackground(ancestor)?ancestor.backgroundImage:style.backgroundImage,backgroundClip:isTextClippedBackground(ancestor)?ancestor.backgroundClip:style.backgroundClip,webkitBackgroundClip:isTextClippedBackground(ancestor)?ancestor.webkitBackgroundClip:style.webkitBackgroundClip,webkitTextStrokeColor:transparentCssPaint(style.webkitTextStrokeColor)?ancestor.webkitTextStrokeColor:style.webkitTextStrokeColor,webkitTextStrokeWidth:parseFloat(style.webkitTextStrokeWidth||"0")>0?style.webkitTextStrokeWidth:ancestor.webkitTextStrokeWidth,textShadow:style.textShadow==="none"?ancestor.textShadow:style.textShadow,filter:style.filter==="none"?ancestor.filter:style.filter}}return style}function elementRenderRect(el,clipped,style,slideRect){if(!rotateFromTransform(style.transform))return null;let scale=scaleFromTransform(style.transform),stageScaleX=(slideRect?.w||1920)/1920,stageScaleY=(slideRect?.h||1080)/1080,cssWidth=parseFloat(style.width||"0")||0,cssHeight=parseFloat(style.height||"0")||0,width=(el.offsetWidth||cssWidth||clipped.width)*scale.x*stageScaleX,height=(el.offsetHeight||cssHeight||clipped.height)*scale.y*stageScaleY;if(!width||!height)return null;let cx=clipped.left+clipped.width/2,cy=clipped.top+clipped.height/2;return{x:cx-width/2,y:cy-height/2,w:width,h:height}}function visualEffectRect(rect,style,slideRect){let outset=shadowOutsetPx(style);return expandedClampedRect(rect,slideRect,outset)}function svgVisualRect(svg,clipped,slideRect){let union={left:clipped.left,top:clipped.top,right:clipped.left+clipped.width,bottom:clipped.top+clipped.height};return svg.querySelectorAll?.("*")?.forEach(child=>{let style=getComputedStyle(child);if(style.display==="none"||style.visibility==="hidden"||Number(style.opacity||1)<=.01)return;let rect=child.getBoundingClientRect();rect.width<=.5||rect.height<=.5||(union.left=Math.min(union.left,rect.left),union.top=Math.min(union.top,rect.top),union.right=Math.max(union.right,rect.right),union.bottom=Math.max(union.bottom,rect.bottom))}),expandedClampedRect({left:union.left,top:union.top,width:union.right-union.left,height:union.bottom-union.top},slideRect,2)}function expandedClampedRect(rect,slideRect,outset=0){let left=Math.max(slideRect.x,rect.left-outset),top=Math.max(slideRect.y,rect.top-outset),right=Math.min(slideRect.x+slideRect.w,rect.left+rect.width+outset),bottom=Math.min(slideRect.y+slideRect.h,rect.top+rect.height+outset);return{left,top,width:Math.max(1,right-left),height:Math.max(1,bottom-top)}}function shadowOutsetPx(style={}){let outset=0;for(let layer of splitShadowLayers(style.boxShadow)){let numbers=layer.replace(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/ig,"").match(/-?\d+(\.\d+)?px/g)||[],offsetX=Math.abs(parseFloat(numbers[0]||"0")||0),offsetY=Math.abs(parseFloat(numbers[1]||"0")||0),blur=Math.abs(parseFloat(numbers[2]||"0")||0),spread=Math.abs(parseFloat(numbers[3]||"0")||0);outset=Math.max(outset,offsetX+offsetY+blur+spread)}let filter=String(style.filter||"");for(let match of filter.matchAll(/blur\(([-\d.]+)px\)|drop-shadow\(([^)]+(?:\)[^)]+)?)\)/ig))if(match[1])outset=Math.max(outset,Math.abs(parseFloat(match[1])||0)*2);else if(match[2]){let numbers=match[2].replace(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/ig,"").match(/-?\d+(\.\d+)?px/g)||[],offsetX=Math.abs(parseFloat(numbers[0]||"0")||0),offsetY=Math.abs(parseFloat(numbers[1]||"0")||0),blur=Math.abs(parseFloat(numbers[2]||"0")||0);outset=Math.max(outset,offsetX+offsetY+blur)}return Math.min(160,Math.ceil(outset))}function splitShadowLayers(value){let raw=String(value||"");return!raw||raw==="none"?[]:splitCssLayers(raw)}function visualScreenshotFallbackKind(el,style,clipped,rawRect,slideRect){if(isTextClippedBackground(style))return null;let tag=el.tagName.toLowerCase();if(["section","main","article","svg","canvas","img","video"].includes(tag))return null;let clipPath=String(style.clipPath||"").trim(),masked=hasCssMask(style),clippedByPath=clipPath&&clipPath!=="none",background=String(style.backgroundImage||""),hasVisualBackground=background&&background!=="none"||hasPaint(style.backgroundColor)||hasAnyBorder(style)||style.boxShadow&&style.boxShadow!=="none";return(masked||clippedByPath)&&hasVisualBackground?"masked-element":shouldScreenshotBlendGroup(el,style,clipped,rawRect,slideRect)?"blend-group":shouldScreenshotRoundedVisual(el,style,clipped,rawRect,slideRect)?"masked-element":shouldScreenshotGradientEffect(el,style,clipped,rawRect,slideRect)?"effect-background":null}function visualScreenshotRect(rawRect,style,slideRect){return visualEffectRect(rawRect,style,slideRect)}function hasCssMask(style={}){let mask=`${style.mask||""} ${style.maskImage||""} ${style.webkitMask||""} ${style.webkitMaskImage||""}`.trim();return!!(mask&&!/^none(?:\s+none)*$/i.test(mask))}function visibleElementChildren(el,slideRect){return[...el.children||[]].filter(child=>{let childStyle=getComputedStyle(child),rect=child.getBoundingClientRect();return childStyle.display!=="none"&&childStyle.visibility!=="hidden"&&Number(childStyle.opacity||1)>.01&&rect.width>2&&rect.height>2&&rect.right>=slideRect.x&&rect.left<=slideRect.x+slideRect.w&&rect.bottom>=slideRect.y&&rect.top<=slideRect.y+slideRect.h})}function shouldScreenshotBlendGroup(el,style,clipped,rawRect,slideRect){let areaRatio=clipped.width*clipped.height/Math.max(1,slideRect.w*slideRect.h);if(areaRatio<=5e-4||areaRatio>.55)return!1;let blendVisualCount=0;for(let child of visibleElementChildren(el,slideRect)){let childStyle=getComputedStyle(child),blend=String(childStyle.mixBlendMode||"");if(!blend||blend==="normal")continue;(String(childStyle.backgroundImage||"").includes("gradient")||hasCssMask(childStyle)||childStyle.filter&&childStyle.filter!=="none")&&(blendVisualCount+=1)}return blendVisualCount>=2}function shouldScreenshotRoundedVisual(el,style,clipped,rawRect,slideRect){let areaRatio=clipped.width*clipped.height/Math.max(1,slideRect.w*slideRect.h);if(areaRatio<=2e-4||areaRatio>.22||hasTransformedAncestor(el))return!1;let background=String(style.backgroundImage||"");if(!(background&&background!=="none"||hasPaint(style.backgroundColor)||hasAnyBorder(style)||style.boxShadow&&style.boxShadow!=="none"))return!1;if(hasNonUniformCssRadius(style,clipped.width,clipped.height))return!0;let radius=maxCssRadius(style,clipped.width,clipped.height),children=visibleElementChildren(el,slideRect);return radius>=6&&(hasAnyBorder(style)||style.boxShadow&&style.boxShadow!=="none")&&children.length>0&&!hasOnlyInlineTextChildren(el)&&!isEditableTextContainer(el)?!0:!hasRoundedClipStyle(style,clipped.width,clipped.height)||!children.length||hasOnlyInlineTextChildren(el)?!1:!!el.querySelector?.('image-slot,[data-dashi-host-image-slot="true"],svg,canvas,img,video')||background.includes("gradient")||style.boxShadow&&style.boxShadow!=="none"}function cornerRadiiPx(style,width=0,height=0){return[cssRadiusPx(style.borderTopLeftRadius,width,height),cssRadiusPx(style.borderTopRightRadius,width,height),cssRadiusPx(style.borderBottomRightRadius,width,height),cssRadiusPx(style.borderBottomLeftRadius,width,height)]}function hasNonUniformCssRadius(style,width=0,height=0){let radii=cornerRadiiPx(style,width,height),max=Math.max(...radii);return max<1?!1:radii.some(radius=>Math.abs(radius-max)>.75)}function hasRoundedClipStyle(style,width=0,height=0){return maxCssRadius(style,width,height)<2?!1:/\b(hidden|clip)\b/i.test(`${style.overflow||""} ${style.overflowX||""} ${style.overflowY||""}`)}function shouldScreenshotGradientEffect(el,style,clipped,rawRect,slideRect){let background=String(style.backgroundImage||"");if(!background.includes("gradient")||backgroundUrl(background)||/repeating-linear-gradient/i.test(background)||(el.innerText||el.textContent||"").trim()||visibleElementChildren(el,slideRect).length)return!1;let areaRatio=clipped.width*clipped.height/Math.max(1,slideRect.w*slideRect.h),hasBlendMode=style.mixBlendMode&&style.mixBlendMode!=="normal",hasFilterEffect=style.filter&&style.filter!=="none",isTransparentRadial=/radial-gradient/i.test(background)&&backgroundHasTransparentStop(background)&&transparentCssPaint(style.backgroundColor),isComplexGradient=/color-mix\(|color\(|closest-side|closest-corner|farthest-side|farthest-corner/i.test(background);return hasBlendMode||hasFilterEffect||hasCssMask(style)?!0:isTransparentRadial&&(isComplexGradient||areaRatio>.005||!sameClientRect(rawRect,clipped))}function shouldUseLocalMaterialFallback(el,style,clipped,slideRect){if(isTextClippedBackground(style)||String(style.clipPath||"").includes("polygon("))return!1;let tag=el.tagName.toLowerCase();if(["section","main","article","svg","canvas","img","video"].includes(tag)||/^h[1-6]$/.test(tag)||["p","li","td","th","blockquote"].includes(tag)||isEditableTextContainer(el)||!(el.innerText||el.textContent||"").trim()&&isInsideEditableTextContainer(el)||el.querySelector?.("svg, canvas, img, video"))return!1;let rawRect=el.getBoundingClientRect(),slideLeft=Number(slideRect.left??slideRect.x??0),slideTop=Number(slideRect.top??slideRect.y??0),slideRight=Number(slideRect.right??slideLeft+Number(slideRect.width??slideRect.w??0)),slideBottom=Number(slideRect.bottom??slideTop+Number(slideRect.height??slideRect.h??0));if(rawRect.left<slideLeft-1||rawRect.top<slideTop-1||rawRect.right>slideRight+1||rawRect.bottom>slideBottom+1||visibleElementChildren(el,slideRect).length&&!hasOnlyInlineTextChildren(el))return!1;let background=String(style.backgroundImage||"");if(!background.includes("gradient")&&!background.includes("url(")||backgroundUrl(background)||shouldUseNativeGradientShape(style,clipped.width,clipped.height))return!1;let areaRatio=clipped.width*clipped.height/Math.max(1,slideRect.w*slideRect.h);return areaRatio<=2e-4||areaRatio>.12?!1:!!(style.boxShadow&&style.boxShadow!=="none"||style.filter&&style.filter!=="none"||style.mixBlendMode&&style.mixBlendMode!=="normal"||maxCssRadius(style,clipped.width,clipped.height)>=10||splitCssLayers(background).filter(layer=>layer.includes("gradient")).length>1)}function isEditableTextContainer(el){return el.isContentEditable||String(el.getAttribute?.("contenteditable")||"").toLowerCase()==="true"||String(el.getAttribute?.("role")||"").toLowerCase()==="textbox"||el.hasAttribute?.("data-editable-id")||el.hasAttribute?.("data-editable-ready")}function isInsideEditableTextContainer(el){let slide=el.closest?.("#deck > .slide");for(let parent=el.parentElement;parent&&parent!==slide?.parentElement;parent=parent.parentElement){if(isEditableTextContainer(parent))return!0;if(parent===slide)break}return!1}function hasOnlyInlineTextChildren(el){return[...el.children||[]].every(child=>isInlineTextOnlyElement(child))}function isInlineTextOnlyElement(el){return!isInlineTextChild(el)||el.querySelector?.("svg, canvas, img, video")?!1:[...el.children||[]].every(child=>isInlineTextOnlyElement(child))}function shouldSkipDecorativeGradientFallback(el,style,clipped,slideRect){let background=String(style.backgroundImage||"");return!background.includes("gradient")||clipped.width*clipped.height/Math.max(1,slideRect.w*slideRect.h)<=.18||(el.innerText||"").trim()||visibleElementChildren(el,slideRect).length||background.includes("radial-gradient")&&transparentCssPaint(style.backgroundColor)&&!String(style.filter||"").includes("blur(")?!1:String(style.filter||"").includes("blur(")||Number(style.opacity||1)<.9}function backgroundHasTransparentStop(value){let raw=String(value||"").toLowerCase();return raw.includes("transparent")||/rgba?\([^)]*,\s*0(?:\.0+)?\s*\)/i.test(raw)||/\/\s*0(?:\.0+)?\s*\)/i.test(raw)}function transparentCssPaint(value){let raw=String(value||"").trim().toLowerCase();return!raw||raw==="transparent"||/^rgba?\(\s*0\s*,\s*0\s*,\s*0\s*,\s*0\s*\)$/.test(raw)}function hasTextPaintSource(style){return isTextClippedBackground(style)||parseFloat(style?.webkitTextStrokeWidth||"0")>0||!transparentCssPaint(style?.webkitTextFillColor)||!transparentCssPaint(style?.color)}function readStyle(el,pseudo=null){let cs=getComputedStyle(el,pseudo),style={};for(let key of STYLE_KEYS){let cssKey=key.startsWith("webkit")?`-${key.replace(/[A-Z]/g,match=>`-${match.toLowerCase()}`)}`:key.replace(/[A-Z]/g,match=>`-${match.toLowerCase()}`);style[key]=cs[key]||cs.getPropertyValue(cssKey)||""}return style}function styleWithCumulativeRotation(style,el){let rotation=cumulativeRotation(el);if(!rotation)return style;let ownRotation=rotateFromTransform(style.transform);return Math.abs(rotation-ownRotation)<.1?style:{...style,transform:`rotate(${rotation}deg)`}}function cumulativeRotation(el){let slide=el?.closest?.("#deck > .slide"),rotation=0;for(let node=el;node instanceof Element&&node!==slide?.parentElement&&(rotation+=rotateFromTransform(getComputedStyle(node).transform),node!==slide);node=node.parentElement);return Math.abs(rotation)<.1?0:rotation}function summarizeCapturedTree(root){let summary={capturedNodes:0,maxDepth:0,textNodes:0,backgroundImages:0,svgImages:0,canvasImages:0,imageNodes:0,shapeCandidates:0};return summarizeNode(root,summary,0),summary}function summarizeNode(node,summary,depth){if(node){summary.capturedNodes+=1,summary.maxDepth=Math.max(summary.maxDepth,depth),node.tag==="#text"&&(summary.textNodes+=1),node.backgroundImageData&&(summary.backgroundImages+=1),node.patternImageData&&(summary.backgroundImages+=1),node.imageKind==="svg"&&(summary.svgImages+=1),node.imageKind==="canvas"&&(summary.canvasImages+=1),node.imageData&&(summary.imageNodes+=1),(hasPaint(node.style?.backgroundColor)||backgroundUrl(node.style?.backgroundImage)||node.style?.backgroundImage?.includes("gradient")||hasAnyBorder(node.style))&&(summary.shapeCandidates+=1);for(let child of node.children||[])summarizeNode(child,summary,depth+1)}}async function elementImageData(img,src){if(!src)return null;if(src.startsWith("data:image/"))return normalizeDataImageUrl(src);try{let canvas=document.createElement("canvas"),width=img.naturalWidth||Math.max(1,Math.round(img.getBoundingClientRect().width)),height=img.naturalHeight||Math.max(1,Math.round(img.getBoundingClientRect().height));return canvas.width=width,canvas.height=height,canvas.getContext("2d").drawImage(img,0,0,width,height),canvas.toDataURL("image/png")}catch{}return fetchImageDataUrl(src)}async function svgElementData(svg,width,height,options={}){try{let clone=cloneSvgWithComputedStyle(svg);options.stripText&&clone.querySelectorAll("text, foreignObject").forEach(el=>el.remove()),clone.setAttribute("xmlns","http://www.w3.org/2000/svg"),clone.setAttribute("width",String(Math.max(1,Math.round(width)))),clone.setAttribute("height",String(Math.max(1,Math.round(height))));let xml=new XMLSerializer().serializeToString(clone),blob=new Blob([xml],{type:"image/svg+xml;charset=utf-8"}),url=URL.createObjectURL(blob);try{let img=await new Promise((resolve,reject)=>{let image=new Image;image.onload=()=>resolve(image),image.onerror=reject,image.src=url}),canvas=document.createElement("canvas");return canvas.width=Math.max(1,Math.round(width*2)),canvas.height=Math.max(1,Math.round(height*2)),canvas.getContext("2d").drawImage(img,0,0,canvas.width,canvas.height),canvas.toDataURL("image/png")}finally{URL.revokeObjectURL(url)}}catch{return null}}function collectSvgTextNodes(svg,slideRect,slideIndex){let svgTextNodes=[...svg.querySelectorAll("text")].map(el=>{let text=normalizeText(el.textContent||"");if(!text)return null;let clipped=clippedRect(el.getBoundingClientRect(),slideRect);if(!clipped||clipped.width<1||clipped.height<1)return null;let style=readStyle(el);return{tag:"#text",source:"svg-text",slideIndex,rect:rectObject(clipped),style,text,singleLine:!/[\r\n]/.test(text),children:[]}}).filter(Boolean),foreignTextNodes=[];return svg.querySelectorAll("foreignObject").forEach(el=>{foreignTextNodes.push(...collectDomFallbackTextNodes(el,slideRect,slideIndex))}),[...svgTextNodes,...foreignTextNodes]}function cloneSvgWithComputedStyle(svg){let clone=svg.cloneNode(!0),source=[svg,...svg.querySelectorAll("*")],target=[clone,...clone.querySelectorAll("*")];return source.forEach((el,index)=>{let cs=getComputedStyle(el),copy=target[index];if(!copy)return;let inline=["fill","stroke","stroke-width","stroke-linecap","stroke-linejoin","opacity","font-family","font-size","font-weight","color"].map(name=>`${name}:${cs.getPropertyValue(name)}`).join(";");copy.setAttribute("style",`${copy.getAttribute("style")||""};${inline}`)}),clone}function patternBackgroundImageData(backgroundImage,width,height,radius=0){let spec=parseRepeatingGradient(backgroundImage);if(!spec)return null;let scale=2,canvas=document.createElement("canvas");canvas.width=Math.max(1,Math.round(width*scale)),canvas.height=Math.max(1,Math.round(height*scale));let ctx=canvas.getContext("2d");if(!ctx)return null;let w=canvas.width,h=canvas.height;ctx.save(),roundedRectPath(ctx,0,0,w,h,Math.max(0,Number(radius||0))*scale),ctx.clip(),ctx.translate(w/2,h/2),ctx.rotate((Number(spec.angle||135)-90)*Math.PI/180);let span=Math.hypot(w,h)*2,period=Math.max(2,spec.period*scale),split=Math.max(1,Math.min(period-1,spec.split*scale));for(let x=-span;x<span;x+=period)ctx.fillStyle=rgbaString(spec.colors[0]),ctx.fillRect(x,-span,split,span*2),ctx.fillStyle=rgbaString(spec.colors[1]),ctx.fillRect(x+split,-span,period-split,span*2);return ctx.restore(),canvas.toDataURL("image/png")}function parseRepeatingGradient(backgroundImage){let raw=String(backgroundImage||"");if(!raw.includes("repeating-linear-gradient"))return null;let angle=Number(raw.match(/repeating-linear-gradient\(\s*([-\d.]+)deg/i)?.[1]||135),matches=[...raw.matchAll(/rgba?\(([^)]+)\)\s+([\d.]+)px/ig)].map(match=>{let parts=match[1].split(",").map(part=>Number(part.trim()));return{color:{r:Math.max(0,Math.min(255,parts[0]||255)),g:Math.max(0,Math.min(255,parts[1]||255)),b:Math.max(0,Math.min(255,parts[2]||255)),a:parts[3]==null?1:Math.max(0,Math.min(1,parts[3]))},stop:Number(match[2]||0)}});if(!matches.length)return{angle,colors:[{r:255,g:255,b:255,a:.04},{r:255,g:255,b:255,a:.016}],split:12,period:24};let stops=[...new Set(matches.map(item=>item.stop).filter(Number.isFinite))].sort((a,b)=>a-b),split=stops.find(stop=>stop>0)||12,period=stops.find(stop=>stop>split)||split*2;return{angle,colors:[matches[0]?.color,matches.at(-1)?.color].filter(Boolean),split,period}}function backgroundCropForClippedRect(rawRect,clipped){let width=rawRect.width||clipped.width,height=rawRect.height||clipped.height,x=Math.max(0,clipped.left-rawRect.left),y=Math.max(0,clipped.top-rawRect.top);return Math.abs(x)<.5&&Math.abs(y)<.5&&Math.abs(clipped.width-width)<.5&&Math.abs(clipped.height-height)<.5?null:{x,y,width:clipped.width,height:clipped.height}}function gradientBackgroundImageData(backgroundImage,width,height,radius=0,crop=null){let layers=splitCssLayers(backgroundImage).filter(layer=>/(?:linear|radial)-gradient/i.test(layer));if(!layers.length)return null;let scale=Math.max(1,Math.min(2,Math.ceil(900/Math.max(width,height)))),canvas=document.createElement("canvas");canvas.width=Math.max(1,Math.round(width*scale)),canvas.height=Math.max(1,Math.round(height*scale));let ctx=canvas.getContext("2d");if(!ctx)return null;let w=canvas.width,h=canvas.height;ctx.save(),roundedRectPath(ctx,0,0,w,h,Math.max(0,Number(radius||0))*scale),ctx.clip();for(let layer of layers.reverse())/radial-gradient/i.test(layer)?drawRadialGradient(ctx,layer,w,h):drawLinearGradient(ctx,layer,w,h);if(ctx.restore(),crop){let cropCanvas=document.createElement("canvas");cropCanvas.width=Math.max(1,Math.round(crop.width*scale)),cropCanvas.height=Math.max(1,Math.round(crop.height*scale));let cropCtx=cropCanvas.getContext("2d");return cropCtx?(cropCtx.drawImage(canvas,Math.round(crop.x*scale),Math.round(crop.y*scale),cropCanvas.width,cropCanvas.height,0,0,cropCanvas.width,cropCanvas.height),cropCanvas.toDataURL("image/png")):null}return canvas.toDataURL("image/png")}function drawLinearGradient(ctx,layer,width,height){let body=layer.replace(/^.*?linear-gradient\(/i,"").replace(/\)\s*$/,""),args=splitCssArgs(body),angle=180,startIndex=0,angleMatch=String(args[0]||"").match(/([-\d.]+)deg/i);if(angleMatch)angle=Number(angleMatch[1]),startIndex=1;else if(/to\s+/i.test(String(args[0]||""))){let dir=String(args[0]).toLowerCase();dir.includes("right")?angle=90:dir.includes("left")?angle=270:dir.includes("top")?angle=0:angle=180,startIndex=1}let stops=normalizeGradientStops(parseGradientColorStops(args.slice(startIndex)),Math.hypot(width,height));if(stops.length<2)return;let theta=(angle-90)*Math.PI/180,dx=Math.cos(theta),dy=Math.sin(theta),len=Math.abs(width*dx)+Math.abs(height*dy),cx=width/2,cy=height/2,gradient=ctx.createLinearGradient(cx-dx*len/2,cy-dy*len/2,cx+dx*len/2,cy+dy*len/2);for(let stop of stops)gradient.addColorStop(stop.offset,rgbaString(stop.color));ctx.fillStyle=gradient,ctx.fillRect(0,0,width,height)}function drawRadialGradient(ctx,layer,width,height){let body=layer.replace(/^.*?radial-gradient\(/i,"").replace(/\)\s*$/,""),args=splitCssArgs(body),startIndex=0,cx=width/2,cy=height/2,first=String(args[0]||""),firstLower=first.toLowerCase();if(!/^(?:rgba?|#)/i.test(first.trim())){let at=first.match(/\bat\s+([-\d.]+)%?\s+([-\d.]+)%?/i);at&&(cx=width*Number(at[1])/100,cy=height*Number(at[2])/100),startIndex=1}let radius=firstLower.includes("closest-side")?Math.max(1,Math.min(cx,cy,width-cx,height-cy)):Math.max(width,height)*.72,stops=normalizeGradientStops(parseGradientColorStops(args.slice(startIndex)),radius);if(stops.length<2)return;let gradient=ctx.createRadialGradient(cx,cy,0,cx,cy,radius);for(let stop of stops)gradient.addColorStop(stop.offset,rgbaString(stop.color));ctx.fillStyle=gradient,ctx.fillRect(0,0,width,height)}function splitCssLayers(value){return splitCssArgs(String(value||"")).filter(Boolean)}function splitCssArgs(value){let out=[],depth=0,quote="",current="";for(let char of String(value||"")){if(quote){current+=char,char===quote&&(quote="");continue}if(char==='"'||char==="'"){quote=char,current+=char;continue}char==="("&&(depth+=1),char===")"&&(depth=Math.max(0,depth-1)),char===","&&depth===0?(out.push(current.trim()),current=""):current+=char}return current.trim()&&out.push(current.trim()),out}function parseGradientColorStops(args){return args.map(parseGradientColorStop).filter(Boolean)}function parseGradientColorStop(value){let raw=String(value||"").trim(),colorMatch=raw.match(/(rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8})/i);return colorMatch?{color:parseCanvasColor(colorMatch[1]),position:parseGradientPosition(raw.slice(colorMatch.index+colorMatch[1].length).trim())}:null}function parseCanvasColor(value){let raw=String(value||"").trim(),hex=raw.match(/^#([0-9a-f]{3,8})$/i);if(hex){let value2=hex[1];return(value2.length===3||value2.length===4)&&(value2=value2.replace(/./g,char=>char+char)),{r:Number.parseInt(value2.slice(0,2),16),g:Number.parseInt(value2.slice(2,4),16),b:Number.parseInt(value2.slice(4,6),16),a:value2.length>=8?Number.parseInt(value2.slice(6,8),16)/255:1}}let rgba=raw.match(/rgba?\(([^)]+)\)/i);if(rgba){let parts=rgba[1].split(",").map(part=>part.trim());return{r:cssColorComponent(parts[0]),g:cssColorComponent(parts[1]),b:cssColorComponent(parts[2]),a:parts[3]==null?1:cssAlpha(parts[3])}}let srgb=raw.match(/^color\(\s*(?:srgb|display-p3)\s+([^)]+)\)$/i);if(srgb){let parts=srgb[1].split("/").map(part=>part.trim()),channels=parts[0].split(/\s+/).filter(Boolean);return{r:cssColorComponent(channels[0],!0),g:cssColorComponent(channels[1],!0),b:cssColorComponent(channels[2],!0),a:parts[1]==null?1:cssAlpha(parts[1])}}return{r:255,g:255,b:255,a:1}}function cssColorComponent(value,unitInterval=!1){let raw=String(value||"").trim();if(raw.endsWith("%"))return Math.max(0,Math.min(255,Number(raw.slice(0,-1))*2.55||0));let number=Number(raw);return unitInterval?Math.max(0,Math.min(255,number*255||0)):Math.max(0,Math.min(255,number||0))}function cssAlpha(value){let raw=String(value||"").trim();return raw.endsWith("%")?Math.max(0,Math.min(1,Number(raw.slice(0,-1))/100||0)):Math.max(0,Math.min(1,Number(raw)||0))}function parseGradientPosition(raw){let match=String(raw||"").match(/([-\d.]+)(%|px)?/);return match?{value:Number(match[1]),unit:match[2]||"%"}:null}function normalizeGradientStops(stops,pixelSpan){let list=stops.filter(stop=>stop?.color);if(!list.length)return[];for(let i=0;i<list.length;i+=1){let position=list[i].position;position?position.unit==="px"?list[i].offset=pixelSpan?position.value/pixelSpan:0:list[i].offset=position.value/100:list[i].offset=list.length===1?0:i/(list.length-1)}list[0].offset=Number.isFinite(list[0].offset)?list[0].offset:0,list[list.length-1].offset=Number.isFinite(list.at(-1).offset)?list.at(-1).offset:1;let last=0;for(let stop of list)stop.offset=Math.max(last,Math.min(1,Number.isFinite(stop.offset)?stop.offset:last)),last=stop.offset;return list}function rgbaString(color){return`rgba(${Math.round(color.r)},${Math.round(color.g)},${Math.round(color.b)},${color.a})`}function roundedRectPath(ctx,x,y,w,h,radius){let r=Math.min(radius,w/2,h/2);ctx.beginPath(),ctx.moveTo(x+r,y),ctx.lineTo(x+w-r,y),ctx.quadraticCurveTo(x+w,y,x+w,y+r),ctx.lineTo(x+w,y+h-r),ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h),ctx.lineTo(x+r,y+h),ctx.quadraticCurveTo(x,y+h,x,y+h-r),ctx.lineTo(x,y+r),ctx.quadraticCurveTo(x,y,x+r,y),ctx.closePath()}function backgroundUrl(backgroundImage){let raw=String(backgroundImage||""),quoted=raw.match(/url\(\s*(['"])(.*?)\1\s*\)/),unquoted=raw.match(/url\(\s*([^)]*?)\s*\)/),value=quoted?.[2]||unquoted?.[1];if(!value)return null;try{return new URL(value,location.href).href}catch{return null}}function isTurbulenceDataImage(url){try{return String(url||"").startsWith("data:image/svg+xml")&&decodeURIComponent(url).includes("feTurbulence")}catch{return String(url||"").includes("feTurbulence")}}function maxCssRadius(style,width=0,height=0){return Math.max(cssRadiusPx(style.borderTopLeftRadius,width,height),cssRadiusPx(style.borderTopRightRadius,width,height),cssRadiusPx(style.borderBottomRightRadius,width,height),cssRadiusPx(style.borderBottomLeftRadius,width,height))}function cssRadiusPx(value,width=0,height=0){let raw=String(value||"").trim();if(!raw||raw==="0px")return 0;if(raw.includes("%")){let pct=parseFloat(raw)||0;return Math.min(Number(width)||0,Number(height)||0)*pct/100}return parseFloat(raw)||0}function finishEditablePptxAnimations(scope){let root=scope||document;try{for(let animation of document.getAnimations({subtree:!0})){let target=animation.effect?.target;if(!(root!==document&&target instanceof Node&&!root.contains(target)))try{animation.updatePlaybackRate?.(1),animation.finish()}catch{try{animation.currentTime=animation.effect?.getTiming?.().duration||999999,animation.pause()}catch{}}}}catch{}}function markUnicornOverlayText(slide,slideRect){slide.querySelectorAll?.("[data-editable-pptx-required-text]")?.forEach(el=>{el.removeAttribute("data-editable-pptx-required-text")});let frames=[...slide.querySelectorAll?.(".bt-unicorn-frame")||[]].filter(el=>isVisibleElement(el,slideRect)).map(el=>el.getBoundingClientRect());if(!frames.length)return;let intersects=(a,b)=>a.left<b.right&&a.right>b.left&&a.top<b.bottom&&a.bottom>b.top,mark=el=>{el&&el.setAttribute("data-editable-pptx-required-text","unicorn-overlay")},walker=document.createTreeWalker(slide,NodeFilter.SHOW_TEXT);for(;walker.nextNode();){if(!normalizeText(walker.currentNode.textContent||""))continue;let parent=walker.currentNode.parentElement;if(!parent||!isVisibleElement(parent,slideRect))continue;let range=document.createRange();range.selectNodeContents(walker.currentNode);let rects=[...range.getClientRects()],bounds=range.getBoundingClientRect();range.detach?.(),(rects.length?rects:[bounds]).some(rect=>rect.width>1&&rect.height>1&&frames.some(frame=>intersects(rect,frame)))&&mark(parent)}slide.querySelectorAll?.("svg text, text")?.forEach(el=>{if(!isVisibleElement(el,slideRect))return;let rect=el.getBoundingClientRect();rect.width>1&&rect.height>1&&frames.some(frame=>intersects(rect,frame))&&mark(el)})}function fallbackTextRisk(root,slideRect){let texts=visibleTextInSubtree(root,slideRect);return{count:texts.length,sample:texts.join(" ").slice(0,160)}}function visibleTextInSubtree(root,slideRect){let texts=[],walk=node=>{for(let child of node.childNodes||[])if(child.nodeType===Node.TEXT_NODE){let text=normalizeText(child.textContent||"");if(!text)continue;let range=document.createRange();range.selectNodeContents(child);let rect=range.getBoundingClientRect();range.detach?.(),rect.width>1&&rect.height>1&&texts.push(text)}else child.nodeType===Node.ELEMENT_NODE&&isVisibleElement(child,slideRect)&&walk(child)};return walk(root),texts}function visibleTextInScreenshotRect(root,slideRect,target=null){let slide=root.closest("#deck > .slide")||root,targetRect=target||root.getBoundingClientRect(),texts=[],seen=new Set,add=(text,rect)=>{let value=normalizeText(text||"");if(!value||rect.width<=1||rect.height<=1||rect.right<=targetRect.left||rect.left>=targetRect.right||rect.bottom<=targetRect.top||rect.top>=targetRect.bottom)return;let key=`${value}:${Math.round(rect.left)}:${Math.round(rect.top)}:${Math.round(rect.width)}:${Math.round(rect.height)}`;seen.has(key)||(seen.add(key),texts.push(value))},walk=node=>{for(let child of node.childNodes||[])if(child.nodeType===Node.TEXT_NODE){let parent=child.parentElement;if(!parent||!isVisibleElement(parent,slideRect))continue;let range=document.createRange();range.selectNodeContents(child);let rects=[...range.getClientRects()],bounds=range.getBoundingClientRect();range.detach?.();for(let rect of rects.length?rects:[bounds])add(child.textContent||"",rect)}else child.nodeType===Node.ELEMENT_NODE&&isVisibleElement(child,slideRect)&&walk(child)};return walk(slide),slide.querySelectorAll?.("svg text, text")?.forEach(el=>{isVisibleElement(el,slideRect)&&add(el.textContent||"",el.getBoundingClientRect())}),{count:texts.length,sample:texts.join(" ").slice(0,160)}}function visibleOverlayPaintInScreenshotRect(root,slideRect,target=null){let slide=root.closest("#deck > .slide")||root,targetRect=target||root.getBoundingClientRect(),items=[],seen=new Set,intersects=(a,b)=>a.left<b.right&&a.right>b.left&&a.top<b.bottom&&a.bottom>b.top,mostlyTargetSized=rect=>{let area=rect.width*rect.height,targetArea=Math.max(1,targetRect.width*targetRect.height);return area/targetArea>.86&&Math.abs(rect.left-targetRect.left)<8&&Math.abs(rect.top-targetRect.top)<8&&Math.abs(rect.right-targetRect.right)<8&&Math.abs(rect.bottom-targetRect.bottom)<8},hasOverlayPaint=el=>{let style=getComputedStyle(el),bg=String(style.backgroundColor||"").trim(),bgImage=String(style.backgroundImage||"").trim(),hasBg=bg&&bg!=="transparent"&&bg!=="rgba(0, 0, 0, 0)",hasBgImage=bgImage&&bgImage!=="none",hasBorder=["Top","Right","Bottom","Left"].some(side=>{let width=parseFloat(style[`border${side}Width`]||"0")||0,color=String(style[`border${side}Color`]||"").trim();return width>0&&color&&color!=="transparent"&&color!=="rgba(0, 0, 0, 0)"});return hasBg||hasBgImage||hasBorder||style.boxShadow&&style.boxShadow!=="none"||["IMG","SVG","CANVAS","VIDEO"].includes(el.tagName)};return slide.querySelectorAll?.("*")?.forEach(el=>{if(el===root||root.contains(el)||el.contains(root)||!isVisibleElement(el,slideRect)||!hasOverlayPaint(el))return;let rect=el.getBoundingClientRect();if(rect.width<=2||rect.height<=2||rect.width*rect.height<120||!intersects(rect,targetRect)||mostlyTargetSized(rect))return;let key=`${el.tagName}:${Math.round(rect.left)}:${Math.round(rect.top)}:${Math.round(rect.width)}:${Math.round(rect.height)}`;seen.has(key)||(seen.add(key),items.push(`${el.tagName.toLowerCase()}${el.className?`.${String(el.className).trim().replace(/\s+/g,".")}`:""}`))}),{count:items.length,sample:items.slice(0,8).join(" ").slice(0,160)}}function collectDomFallbackTextNodes(root,slideRect,slideIndex){let nodes=[],walk=node=>{for(let child of node.childNodes||[])if(child.nodeType===Node.TEXT_NODE){let parent=child.parentElement;if(!parent||!isVisibleElement(parent,slideRect))continue;let textNode=captureTextNode(child,parent,slideRect,readStyle(parent),slideIndex);Array.isArray(textNode)?nodes.push(...textNode):textNode&&nodes.push(textNode)}else child.nodeType===Node.ELEMENT_NODE&&isVisibleElement(child,slideRect)&&walk(child)};return walk(root),nodes}function svgTextRisk(svg){let texts=[...svg.querySelectorAll("text, foreignObject")].map(el=>normalizeText(el.textContent||"")).filter(Boolean);return{count:texts.length,sample:texts.join(" ").slice(0,160)}}async function fetchImageDataUrl(url,width=0,height=0,alpha=1){let inner=(async()=>{try{if(url.startsWith("data:image/svg+xml"))return await rasterizeSvgDataUrl(url,width,height,alpha);if(url.startsWith("data:image/"))return normalizeDataImageUrl(url);let response=await fetch(url,{credentials:new URL(url,location.href).origin===location.origin?"same-origin":"omit"});if(!response.ok)return null;let blob=await response.blob();if(!blob.type.startsWith("image/"))return null;let dataUrl=await blobToDataUrl(blob);return blob.type==="image/svg+xml"?await rasterizeSvgDataUrl(dataUrl,width,height,alpha):dataUrl}catch{return null}})(),timeout=new Promise(resolve=>setTimeout(()=>resolve(null),15e3));return Promise.race([timeout,inner])}function normalizeDataImageUrl(url){let raw=String(url||"");if(!raw.startsWith("data:image/")||/^data:image\/[^;,]+;base64,/i.test(raw))return raw;let match=raw.match(/^data:(image\/[^;,]+)(?:;charset=[^,]+)?,(.*)$/i);if(!match)return raw;try{let decoded=decodeURIComponent(match[2]);return`data:${match[1]};base64,${btoa(unescape(encodeURIComponent(decoded)))}`}catch{return raw}}function rasterizeSvgDataUrl(url,width=0,height=0,alpha=1){return new Promise(resolve=>{let timer=setTimeout(()=>resolve(normalizeDataImageUrl(url)),1e4),img=new Image;img.onload=()=>{clearTimeout(timer);try{let canvas=document.createElement("canvas");canvas.width=Math.max(1,Math.round(width||img.naturalWidth||160)),canvas.height=Math.max(1,Math.round(height||img.naturalHeight||160));let ctx=canvas.getContext("2d");ctx.globalAlpha=Math.max(0,Math.min(1,Number(alpha)||0)),ctx.drawImage(img,0,0,canvas.width,canvas.height),resolve(canvas.toDataURL("image/png"))}catch{resolve(normalizeDataImageUrl(url))}},img.onerror=()=>{clearTimeout(timer),resolve(normalizeDataImageUrl(url))},img.src=normalizeDataImageUrl(url)})}function blobToDataUrl(blob){return new Promise(resolve=>{setTimeout(()=>resolve(null),1e4);let reader=new FileReader;reader.onload=()=>resolve(reader.result),reader.onerror=()=>resolve(null),reader.readAsDataURL(blob)})}function isVisibleElement(el,slideRect,style=getComputedStyle(el)){if(!(el instanceof Element)||style.display==="none"||style.visibility==="hidden"||Number(style.opacity||1)<=.01)return!1;let rect=el.getBoundingClientRect();return!(rect.width<=.5||rect.height<=.5||rect.right<slideRect.x||rect.left>slideRect.x+slideRect.w||rect.bottom<slideRect.y||rect.top>slideRect.y+slideRect.h)}function isMediaChrome(el){return!!el.closest("script,style,noscript,template,#nav,#preview-panel,#slide-rail,.theme03-theme-toggle,.ctl,.spill,input")}function shouldScreenshotImageSlot(el){return isImageSlotElement(el)&&!hasTransformedAncestor(el)}function isImageSlotElement(el){return!!el.matches?.('image-slot,[data-dashi-host-image-slot="true"]')}function hasTransformedAncestor(el){let slide=el.closest?.("#deck > .slide");for(let parent=el.parentElement;parent&&parent!==slide;parent=parent.parentElement){let transform=String(getComputedStyle(parent).transform||"");if(isRotatedOrSkewedTransform(transform))return!0}return!1}function isRotatedOrSkewedTransform(transform){let raw=String(transform||"").trim();if(!raw||raw==="none")return!1;let matrix=raw.match(/^matrix\(([^)]+)\)$/);if(matrix){let[a,b,c,d]=matrix[1].split(",").map(value=>Number.parseFloat(value.trim())||0);return Math.abs(b)>.001||Math.abs(c)>.001||Math.abs(a-d)>.001}let matrix3d=raw.match(/^matrix3d\(([^)]+)\)$/);if(matrix3d){let values=matrix3d[1].split(",").map(value=>Number.parseFloat(value.trim())||0);return Math.abs(values[1])>.001||Math.abs(values[4])>.001||Math.abs(values[0]-values[5])>.001}return/rotate|skew/i.test(raw)}function clippedRect(rect,slideRect){let left=Math.max(rect.left,slideRect.x),top=Math.max(rect.top,slideRect.y),right=Math.min(rect.right,slideRect.x+slideRect.w),bottom=Math.min(rect.bottom,slideRect.y+slideRect.h);return right<=left||bottom<=top?null:{left,top,width:right-left,height:bottom-top}}function slideClipRect(slideRect){let left=Number(slideRect.x||0),top=Number(slideRect.y||0),width=Number(slideRect.w||slideRect.width||0),height=Number(slideRect.h||slideRect.height||0);return{left,top,right:left+width,bottom:top+height,width,height}}function intersectClientRect(rect,clipRect){if(!rect||!clipRect)return null;let rectLeft=Number(rect.left??rect.x??0),rectTop=Number(rect.top??rect.y??0),rectRight=Number(rect.right??rectLeft+Number(rect.width??rect.w??0)),rectBottom=Number(rect.bottom??rectTop+Number(rect.height??rect.h??0)),clipLeft=Number(clipRect.left??clipRect.x??0),clipTop=Number(clipRect.top??clipRect.y??0),clipRight=Number(clipRect.right??clipLeft+Number(clipRect.width??clipRect.w??0)),clipBottom=Number(clipRect.bottom??clipTop+Number(clipRect.height??clipRect.h??0)),left=Math.max(rectLeft,clipLeft),top=Math.max(rectTop,clipTop),right=Math.min(rectRight,clipRight),bottom=Math.min(rectBottom,clipBottom);return right<=left||bottom<=top?null:{left,top,right,bottom,width:right-left,height:bottom-top}}function nextChildClipRect(el,style,rawRect,currentClip){return hasClipStyle(style)&&intersectClientRect(rawRect||el.getBoundingClientRect(),currentClip)||currentClip}function hasClipStyle(style){let overflow=String(style?.overflow||"").trim().toLowerCase();if(overflow&&overflow!=="visible")return!0;let clipPath=String(style?.clipPath||"").trim();if(clipPath&&clipPath!=="none")return!0;let mask=`${style?.mask||""} ${style?.maskImage||""} ${style?.webkitMask||""} ${style?.webkitMaskImage||""}`.trim();return!!(mask&&!/^none(?:\s+none)*$/i.test(mask))}function sameClientRect(a,b,tolerance=.5){if(!a||!b)return!1;let aLeft=Number(a.left??a.x??0),aTop=Number(a.top??a.y??0),aRight=Number(a.right??aLeft+Number(a.width??a.w??0)),aBottom=Number(a.bottom??aTop+Number(a.height??a.h??0)),bLeft=Number(b.left??b.x??0),bTop=Number(b.top??b.y??0),bRight=Number(b.right??bLeft+Number(b.width??b.w??0)),bBottom=Number(b.bottom??bTop+Number(b.height??b.h??0));return Math.abs(aLeft-bLeft)<=tolerance&&Math.abs(aTop-bTop)<=tolerance&&Math.abs(aRight-bRight)<=tolerance&&Math.abs(aBottom-bBottom)<=tolerance}function rectObject(rect){return{x:rect.left??rect.x,y:rect.top??rect.y,w:rect.width??rect.w,h:rect.height??rect.h}}function normalizeText(value){return String(value||"").replace(/\s+/g," ").trim()}function hasPaint(color){let raw=String(color||"").trim();return raw&&raw!=="transparent"&&!/^rgba?\(\s*0\s*,\s*0\s*,\s*0\s*,\s*0\s*\)$/i.test(raw)}function hasAnyBorder(style){return["Top","Right","Bottom","Left"].some(side=>parseFloat(style?.[`border${side}Width`]||"0")>0&&hasPaint(style?.[`border${side}Color`]))}function cssPx(value){let raw=String(value||"").trim();if(!raw||raw==="auto")return null;let n=parseFloat(raw);return Number.isFinite(n)?n:null}function cssLengthPx(value,base=0){let raw=String(value||"").trim();if(!raw||raw==="auto")return null;let n=parseFloat(raw);return Number.isFinite(n)?raw.endsWith("%")?n/100*(Number(base)||0):n:null}function translateFromTransform(value,width=0,height=0){let raw=String(value||"");if(!raw||raw==="none")return{x:0,y:0};let matrix3d=raw.match(/matrix3d\(([^)]+)\)/i);if(matrix3d){let parts=splitCssArgs(matrix3d[1]).map(Number);return{x:Number.isFinite(parts[12])?parts[12]:0,y:Number.isFinite(parts[13])?parts[13]:0}}let matrix=raw.match(/matrix\(([^)]+)\)/i);if(matrix){let parts=splitCssArgs(matrix[1]).map(Number);return{x:Number.isFinite(parts[4])?parts[4]:0,y:Number.isFinite(parts[5])?parts[5]:0}}let translate=raw.match(/translate(?:3d)?\(([^)]+)\)/i),translateX=raw.match(/translateX\(([^)]+)\)/i),translateY=raw.match(/translateY\(([^)]+)\)/i),out={x:0,y:0};if(translate){let[x="0",y="0"]=splitCssArgs(translate[1]);out.x+=cssLengthPx(x,width)||0,out.y+=cssLengthPx(y,height)||0}return translateX&&(out.x+=cssLengthPx(translateX[1],width)||0),translateY&&(out.y+=cssLengthPx(translateY[1],height)||0),out}function isTextClippedBackground(style){return`${style?.backgroundClip||""} ${style?.webkitBackgroundClip||""}`.toLowerCase().includes("text")}function shouldUseNativeGradientShape(style={},width=0,height=0){let background=String(style.backgroundImage||"");if(!/linear-gradient/i.test(background)||/repeating-linear-gradient/i.test(background))return!1;let gradientLayers=splitCssLayers(background).filter(layer=>/(?:linear|radial)-gradient/i.test(layer));if(gradientLayers.length===1&&isLowAlphaLinearGradient(gradientLayers[0]))return!0;let minSide=Math.min(Number(width)||0,Number(height)||0),maxSide=Math.max(Number(width)||0,Number(height)||0);return minSide>0&&minSide<=16&&maxSide>=24}function isLowAlphaLinearGradient(layer){if(!/linear-gradient/i.test(String(layer||""))||/radial-gradient/i.test(String(layer||"")))return!1;let body=String(layer).replace(/^.*?linear-gradient\(/i,"").replace(/\)\s*$/,""),args=splitCssArgs(body),startIndex=0;/([-\d.]+deg|to\s+)/i.test(String(args[0]||""))&&(startIndex=1);let stops=parseGradientColorStops(args.slice(startIndex));if(stops.length<2||!stops.every(stop=>stop.color&&stop.color.a>0&&stop.color.a<=.28))return!1;let[first]=stops;return stops.every(stop=>{let color=stop.color;return Math.abs(color.r-first.color.r)<=24&&Math.abs(color.g-first.color.g)<=24&&Math.abs(color.b-first.color.b)<=24})}function rotateFromTransform(value){let raw=String(value||"");if(!raw||raw==="none")return 0;let rotate=raw.match(/rotate\(\s*([-\d.]+)deg\s*\)/i);if(rotate)return Number(rotate[1])||0;let matrix=raw.match(/matrix\(([^)]+)\)/);if(!matrix)return 0;let[a,b]=matrix[1].split(",").map(Number);return!Number.isFinite(a)||!Number.isFinite(b)?0:Math.round(Math.atan2(b,a)*180/Math.PI)}function scaleFromTransform(value){let matrix=String(value||"").match(/matrix\(([^)]+)\)/);if(!matrix)return{x:1,y:1};let[a,b,c,d]=matrix[1].split(",").map(Number);return{x:Number.isFinite(a)&&Number.isFinite(b)&&Math.hypot(a,b)||1,y:Number.isFinite(c)&&Number.isFinite(d)&&Math.hypot(c,d)||1}}var restoreHiddenScreenshotStyles=token=>{let entries=window.__editablePptxHiddenTextStyles?.get(token)||[];for(let entry of entries)entry.style==null?entry.el.removeAttribute("style"):entry.el.setAttribute("style",entry.style);window.__editablePptxHiddenTextStyles?.delete(token)},hideNodeForScreenshot=({exportId,mode,stripText,stripOverlay,screenshotRect})=>{let root=document.querySelector(`#deck > .slide.active [data-editable-pptx-export-id="${exportId}"], #deck > .slide[data-deck-active] [data-editable-pptx-export-id="${exportId}"]`)||document.querySelector(`[data-editable-pptx-export-id="${exportId}"]`);if(!root)return null;let token=`hide-${Date.now()}-${Math.random().toString(36).slice(2)}`,entries=[],rootRect=root.getBoundingClientRect(),targetRect=screenshotRect?{left:Number(screenshotRect.x||0),top:Number(screenshotRect.y||0),right:Number(screenshotRect.x||0)+Number(screenshotRect.w||0),bottom:Number(screenshotRect.y||0)+Number(screenshotRect.h||0),width:Number(screenshotRect.w||0),height:Number(screenshotRect.h||0)}:rootRect,slide=mode==="screenshot-rect"?root.closest("#deck > .slide")||document.querySelector("#deck > .slide.active, #deck > .slide[data-deck-active]"):root,intersects=(a,b)=>a.left<b.right&&a.right>b.left&&a.top<b.bottom&&a.bottom>b.top,remember=el=>{!el||entries.some(entry=>entry.el===el)||entries.push({el,style:el.getAttribute("style")})},mark=el=>{if(!el)return;remember(el);let style=getComputedStyle(el);el.style.setProperty("color","transparent","important"),el.style.setProperty("-webkit-text-fill-color","transparent","important"),el.style.setProperty("-webkit-text-stroke-color","transparent","important"),el.style.setProperty("text-shadow","none","important"),el.style.setProperty("text-decoration-color","transparent","important"),el.style.setProperty("fill","transparent","important"),el.style.setProperty("stroke","transparent","important"),(String(style.backgroundClip||"").includes("text")||String(style.webkitBackgroundClip||"").includes("text"))&&el.style.setProperty("background-image","none","important")},markOverlay=el=>{el&&(remember(el),el.style.setProperty("opacity","0","important"))},hasOverlayPaint=el=>{let style=getComputedStyle(el),bg=String(style.backgroundColor||"").trim(),bgImage=String(style.backgroundImage||"").trim(),hasBg=bg&&bg!=="transparent"&&bg!=="rgba(0, 0, 0, 0)",hasBgImage=bgImage&&bgImage!=="none",hasBorder=["Top","Right","Bottom","Left"].some(side=>{let width=parseFloat(style[`border${side}Width`]||"0")||0,color=String(style[`border${side}Color`]||"").trim();return width>0&&color&&color!=="transparent"&&color!=="rgba(0, 0, 0, 0)"});return hasBg||hasBgImage||hasBorder||style.boxShadow&&style.boxShadow!=="none"||["IMG","SVG","CANVAS","VIDEO"].includes(el.tagName)},isMostlyRootSized=rect=>{let area=rect.width*rect.height,targetArea=Math.max(1,targetRect.width*targetRect.height);return area/targetArea>.86&&Math.abs(rect.left-targetRect.left)<8&&Math.abs(rect.top-targetRect.top)<8&&Math.abs(rect.right-targetRect.right)<8&&Math.abs(rect.bottom-targetRect.bottom)<8},isVisible=el=>{let style=getComputedStyle(el),rect=el.getBoundingClientRect();return style.display!=="none"&&style.visibility!=="hidden"&&Number(style.opacity||1)>.01&&rect.width>2&&rect.height>2};mode==="screenshot-rect"&&stripOverlay&&(slide||root).querySelectorAll("*").forEach(el=>{if(el===root||root.contains(el)||el.contains(root)||!isVisible(el))return;let rect=el.getBoundingClientRect();!intersects(rect,targetRect)||isMostlyRootSized(rect)||!hasOverlayPaint(el)||markOverlay(el)});let shouldHideRange=range=>{if(mode!=="screenshot-rect")return!0;let rects=[...range.getClientRects()],bounds=range.getBoundingClientRect();return(rects.length?rects:[bounds]).some(rect=>rect.width>1&&rect.height>1&&intersects(rect,targetRect))};if(stripText){let walker=document.createTreeWalker(slide||root,NodeFilter.SHOW_TEXT);for(;walker.nextNode();){if(!(walker.currentNode.textContent||"").trim())continue;let range=document.createRange();range.selectNodeContents(walker.currentNode);let shouldHide=shouldHideRange(range);range.detach?.(),shouldHide&&mark(walker.currentNode.parentElement)}(slide||root).querySelectorAll("svg text, text").forEach(el=>{if(mode!=="screenshot-rect"){mark(el);return}let rect=el.getBoundingClientRect();rect.width>1&&rect.height>1&&intersects(rect,targetRect)&&mark(el)})}return window.__editablePptxHiddenTextStyles||=new Map,window.__editablePptxHiddenTextStyles.set(token,entries),token};function getExportSlideDescriptors(mode="comparison"){let outputMode=mode==="selected-only"?"selected-only":"comparison",visibleSlides=window.__getVisibleSlides?.()||[...document.querySelectorAll("#deck > .slide:not([hidden])")],requestedItems=typeof window.__getExportSlides=="function"?window.__getExportSlides(outputMode):visibleSlides,descriptors=(Array.isArray(requestedItems)?requestedItems:visibleSlides).map((item,index)=>{let slide=item?.dataset?item:null,sourceSlideId=String(item?.sourceSlideId||slide?.dataset?.vmSourceSlideId||slide?.dataset?.vmSlideId||slide?.id||""),sourceIndex=sourceSlideId?visibleSlides.findIndex(candidate=>candidate?.dataset?.vmSourceSlideId===sourceSlideId||candidate?.dataset?.vmSlideId===sourceSlideId||candidate?.id===sourceSlideId):-1,requestedLogicalIndex=Number(item?.logicalIndex),logicalIndex=slide?visibleSlides.indexOf(slide):sourceIndex>=0?sourceIndex:Number.isInteger(requestedLogicalIndex)?requestedLogicalIndex:-1;return{sourceSlideId,logicalIndex,variantStateId:String(item?.variantStateId||item?.stateId||slide?.dataset?.vmVariantStateId||slide?.dataset?.vmSlideId||slide?.id||""),variantKind:String(item?.variantKind||slide?.dataset?.vmVariantKind||""),exportIndex:Number.isInteger(Number(item?.exportIndex??item?.comparisonIndex))?Number(item.exportIndex??item.comparisonIndex):index}}),invalid=descriptors.find(descriptor=>descriptor.logicalIndex<0||descriptor.logicalIndex>=visibleSlides.length);if(invalid)throw new Error(`Export descriptor source slide "${invalid.sourceSlideId||invalid.logicalIndex}" is not visible.`);return descriptors}async function materializeExportSlideDescriptor(descriptor){let visibleSlides=window.__getVisibleSlides?.()||[...document.querySelectorAll("#deck > .slide:not([hidden])")],sourceSlideId=String(descriptor?.sourceSlideId||""),sourceIndex=sourceSlideId?visibleSlides.findIndex(slide2=>slide2?.dataset?.vmSourceSlideId===sourceSlideId||slide2?.dataset?.vmSlideId===sourceSlideId||slide2?.id===sourceSlideId):-1,requestedLogicalIndex=Number(descriptor?.logicalIndex),logicalIndex=sourceIndex>=0?sourceIndex:Number.isInteger(requestedLogicalIndex)?requestedLogicalIndex:-1,slide=visibleSlides[logicalIndex];if(!slide)throw new Error(`Export source slide "${sourceSlideId||requestedLogicalIndex}" is not visible.`);let selections=window.__deckViewModel?.peek?.("variantSelection")||window.__deckViewModel?.getState?.().variantSelection||{},previousVariantStateId=String(selections[sourceSlideId]||slide?.dataset?.vmVariantStateId||slide?.dataset?.vmSlideId||""),targetVariantStateId=String(descriptor?.variantStateId||""),currentVariantStateId=String(slide?.dataset?.vmVariantStateId||slide?.dataset?.vmSlideId||""),token={sourceSlideId,logicalIndex,previousVariantStateId,targetVariantStateId,changed:!!(targetVariantStateId&&targetVariantStateId!==currentVariantStateId)};if(window.go?.(logicalIndex,{animate:!1,force:!0}),token.changed){try{let rendered;if(typeof window.__materializeExportSlide=="function")rendered=await window.__materializeExportSlide({...descriptor,sourceSlideId,logicalIndex,variantStateId:targetVariantStateId});else if(typeof window.__setSlideVariant=="function")rendered=await window.__setSlideVariant(sourceSlideId,targetVariantStateId);else throw new Error("Deck runtime does not support export variant materialization.");if(rendered===!1)throw new Error(`Could not materialize export variant "${targetVariantStateId}".`);rendered&&typeof rendered=="object"&&(token.runtimeRestoreToken=rendered)}catch(error){try{typeof window.__restoreExportSlide=="function"?await window.__restoreExportSlide(token.runtimeRestoreToken||{...token,variantStateId:previousVariantStateId}):previousVariantStateId&&typeof window.__setSlideVariant=="function"&&await window.__setSlideVariant(sourceSlideId,previousVariantStateId),window.go?.(logicalIndex,{animate:!1,force:!0})}catch{}throw error}window.go?.(logicalIndex,{animate:!1,force:!0})}return token}async function restoreExportSlideDescriptor(token){if(!token?.changed)return!1;let restored;if(typeof window.__restoreExportSlide=="function")restored=await window.__restoreExportSlide(token.runtimeRestoreToken||{...token,variantStateId:token.previousVariantStateId});else if(token.previousVariantStateId&&typeof window.__setSlideVariant=="function")restored=await window.__setSlideVariant(token.sourceSlideId,token.previousVariantStateId);else throw new Error(`Deck runtime cannot restore source slide "${token.sourceSlideId||""}".`);if(restored===!1)throw new Error(`Could not restore export variant "${token.previousVariantStateId||""}".`);return window.go?.(token.logicalIndex,{animate:!1,force:!0}),!0}var applyEditablePptxSnapshot=async snapshot=>{window.__applyEditablePptxSnapshotText=function(scope){let textState=window.__editablePptxSnapshotTextState;if(!scope||!textState||typeof textState!="object")return;[...scope.dataset?.editableId?[scope]:[],...scope.querySelectorAll?.("[data-editable-id]")||[]].forEach(el=>{let synced=el.dataset.syncText?textState[`sync:${el.dataset.syncText}`]:void 0,value=synced!==void 0?synced:textState[el.dataset.editableId];value!==void 0&&(el.innerHTML=value)})};let state=snapshot?.state||{};snapshot?.themePack!==void 0&&window.__setActiveThemePack?.(snapshot.themePack||"",{navigate:!1}),Array.isArray(state.duplicatedSlides)&&(state.duplicatedSlides.forEach(record=>window.__deckViewModel?.restoreDuplicatedSlide?.(record)),window.__restorePersistedCatalogSlides?.()),Array.isArray(state.slideOrder)&&window.__deckViewModel?.setSlideOrder?.(state.slideOrder),Array.isArray(state.skippedSlides)&&window.__deckViewModel?.setSkippedSlides?.(state.skippedSlides),Array.isArray(state.deletedSlides)&&window.__deckViewModel?.setDeletedSlides?.(state.deletedSlides),window.__applyPersistedSlideOrder?.(),window.__refreshSlideList?.(),state.variantSelection&&typeof state.variantSelection=="object"&&(typeof window.__deckViewModel?.setVariantSelections=="function"?window.__deckViewModel.setVariantSelections(state.variantSelection):typeof window.__deckViewModel?.setVariantSelection=="function"&&Object.entries(state.variantSelection).forEach(([slideId,variantId])=>{window.__deckViewModel.setVariantSelection(slideId,variantId)}),await window.__applyDeckVariantSelections?.()),state.text&&typeof state.text=="object"&&window.__deckViewModel?.setTextState?.(state.text),window.__editablePptxSnapshotTextState=state.text&&typeof state.text=="object"?state.text:{},state.props&&typeof state.props=="object"&&Object.entries(state.props).forEach(([slideId,props])=>window.__deckViewModel?.setProps?.(slideId,props)),window.__syncDeckViewModelFromDom?.(),window.__layoutDeck?.();let slides=window.__getVisibleSlides?.()||[...document.querySelectorAll("#deck > .slide:not([hidden])")];slides.forEach(slide=>{window.__ensureRuntimeSlideRendered?.(slide),window.__applyEditablePptxSnapshotText?.(slide)}),Array.isArray(snapshot?.canvasSnapshots)&&snapshot.canvasSnapshots.forEach(item=>{let slide=findSlideByStateId(item.slideStateId)||findSlideByStateId(item.sourceSlideId)||slides[item.slideIndex];if(!slide||!item?.data)return;let original=slide.querySelectorAll?.("canvas")?.[item.canvasIndex];original&&(original.style.display="none");let img=document.createElement("img");img.src=item.data,img.setAttribute("data-editable-pptx-canvas-snapshot",""),Object.assign(img.style,{position:"absolute",left:`${item.left}%`,top:`${item.top}%`,width:`${item.width}%`,height:`${item.height}%`,zIndex:"2147480000",pointerEvents:"none"}),slide.appendChild(img)});let currentSlide=findSlideByStateId(snapshot?.currentSlideStateId)||findSlideByStateId(snapshot?.currentSourceSlideId),index=currentSlide?slides.indexOf(currentSlide):Math.max(0,Math.min(slides.length-1,Number(snapshot?.currentIndex||0)));window.go?.(index,{animate:!1,force:!0}),await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)));function findSlideByStateId(stateId){return stateId&&slides.find(slide=>slide?.dataset?.vmVariantStateId===stateId||slide?.dataset?.vmSourceSlideId===stateId||slide?.dataset?.vmSlideId===stateId||slide?.id===stateId)||null}};var imageAdapter=null;function configureImageAdapter(adapter){imageAdapter=adapter}function adaptTransparentPng(dataUrl,options={}){if(!imageAdapter?.normalizeTransparentPng)return dataUrl;try{return imageAdapter.normalizeTransparentPng(dataUrl,options)}catch{return dataUrl}}var SOURCE_W=1920,SOURCE_H=1080,PPT_W=16,PPT_H=9,PX_TO_PT=.75,PPTX_SAFE_FONT_MAP=[[/^(Noto Sans SC|PingFang SC|Source Han Sans.*|思源黑体|黑体)$/i,"Microsoft YaHei"],[/^(Songti SC|宋体|Noto Serif SC|Source Han Serif.*|思源宋体)$/i,"SimSun"],[/^Anton$/i,"Impact"],[/^(Space Mono|IBM Plex Mono|JetBrains Mono|SFMono.*|ui-monospace|monospace|Menlo|Consolas)$/i,"Courier New"],[/^(Newsreader.*|serif)$/i,"Georgia"],[/^Caveat$/i,"Georgia"],[/^(Archivo.*|Space Grotesk|IBM Plex Sans|Inter|Arimo|sans-serif|system-ui|-apple-system)$/i,"Arial"]];async function emitProgress(onProgress,update){if(typeof onProgress=="function")try{await onProgress(update)}catch{}}function shouldUseAlphaMatteScreenshot(node){return isBrowserVisualImageKind(node?.imageKind)}function shouldApplyNodeRadiusAlphaMask(node){return!isBrowserVisualImageKind(node?.imageKind)}function walkCapturedNodes(node,visit){if(node){visit(node);for(let child of node.children||[])walkCapturedNodes(child,visit)}}function screenshotClip(rect){return{x:Math.max(0,Math.floor(Number(rect.x||0))),y:Math.max(0,Math.floor(Number(rect.y||0))),width:Math.max(1,Math.ceil(Number(rect.w??rect.width??1))),height:Math.max(1,Math.ceil(Number(rect.h??rect.height??1)))}}function renderCapturedNode(slide,node,slideRect,warnings,totals){if(!(!node||node.style?.display==="none"||node.style?.visibility==="hidden")&&!(Number(node.style?.opacity||1)<=.01)&&!(!node.rect||node.rect.w<.5||node.rect.h<.5)){if(node.tag==="#text"){renderText(slide,node,slideRect,warnings,totals);return}if(renderBox(slide,node,slideRect,warnings,totals),renderNodeImage(slide,node,slideRect,warnings,totals),node.tag==="pseudo"&&node.text&&renderText(slide,{...node,tag:"#text",singleLine:!0},slideRect,warnings,totals),!(node.tag==="img"||node.tag==="canvas"))for(let child of sortedChildrenForRendering(node.children||[]))renderCapturedNode(slide,child,slideRect,warnings,totals)}}function sortedChildrenForRendering(children){return children.map((child,index)=>({child,index})).sort((a,b)=>stackingOrder(a.child)-stackingOrder(b.child)||a.index-b.index).map(item=>item.child)}function stackingOrder(node){let z=String(node?.style?.zIndex||"").trim();if(!z||z==="auto")return 0;let value=Number.parseFloat(z);return Number.isFinite(value)?value:0}function renderBox(slide,node,slideRect,warnings,totals){if(isBrowserVisualImageKind(node.imageKind))return;let c=coords(node,slideRect);if(c.w<.003||c.h<.003)return;let style=node.style||{};if(shouldSkipNativeDecorativeBlurBox(node,c,style)){warnings.push({slide:node.slideIndex,type:"decorative-blur-shape-skipped",tag:node.tag});return}let borderTriangle=cssBorderTriangle(style,c),hasLocalBackgroundImage=node.backgroundImageData||node.patternImageData,polygonPoints=borderTriangle?.points||cssClipPolygonPoints(style.clipPath,c),allowGradientFillApproximation=!hasLocalBackgroundImage&&!shouldSuppressGradientFillApproximation(node,style,c),fill=borderTriangle?.fill||(isTextClippedBackground(style)?parseCssColor(style.backgroundColor):parseCssColor(style.backgroundColor)||(allowGradientFillApproximation?colorFromBackgroundImage(style.backgroundImage):null)),radiusPx=maxCssRadius(style,node.rect?.w||0,node.rect?.h||0),radius=Math.min(radiusPx,48)/slideRect.w*PPT_W,borders=readBorders(style),hasBorder=borders.some(border=>border.width>0&&border.color),uniformBorder=uniformBorderStyle(borders),shadow=parseBoxShadow(style.boxShadow),rotate=rotateFromTransform(style.transform)||0,hasFill=fill&&fill.alpha>.01;if(isTinyRotatedBorderOnlyPseudo(node,c,hasFill,hasBorder,rotate)){warnings.push({slide:node.slideIndex,type:"decorative-pseudo-border-skipped",count:1});return}let isLargeGradient=fill?.gradient&&c.w>PPT_W*.72&&c.h>PPT_H*.72,isNarrowGradientLine=fill?.gradient&&Math.min(c.w,c.h)<=.12&&Math.max(c.w,c.h)>=.35,isDecorativeGradient=fill?.gradient&&!isLargeGradient&&!isNarrowGradientLine&&!(node.children||[]).length,fillAlpha=isDecorativeGradient?Math.min(fill.alpha,.08):fill?.alpha,forceRectForThinPill=radius>.02&&!hasBorder&&Math.min(c.w,c.h)<=.16&&Math.max(c.w,c.h)>=.35,shapeName=polygonPoints?"custGeom":isCircleLikeBox(node,radiusPx)?"ellipse":forceRectForThinPill?"rect":isDecorativeGradient&&radius>Math.min(c.w,c.h)*.2?"ellipse":radius>.02?"roundRect":"rect",firstBorder=borders.find(border=>border.color),line=borderTriangle?{color:fill?.color||"FFFFFF",transparency:100}:hasBorder&&rotate?{color:firstBorder?.color||fill?.color||"FFFFFF",transparency:combinedTransparency(firstBorder?.alpha||1,style.opacity),width:Math.max(...borders.map(border=>border.width||0))*PX_TO_PT}:hasBorder&&uniformBorder?{color:uniformBorder.color,transparency:combinedTransparency(uniformBorder.alpha,style.opacity),width:uniformBorder.width*PX_TO_PT}:{color:hasBorder&&(firstBorder?.color||fill?.color)||"FFFFFF",transparency:100};if(hasFill||hasBorder||borderTriangle)try{slide.addShape(shapeName,{...c,fill:hasFill?{color:fill.color,transparency:combinedTransparency(fillAlpha,style.opacity)}:{color:"FFFFFF",transparency:100},line,rectRadius:shapeName==="roundRect"&&radius||void 0,points:polygonPoints||void 0,shadow:hasFill&&shadow?shadow:void 0,rotate:rotate||void 0}),totals.shapeObjects+=1}catch{warnings.push({slide:node.slideIndex,type:"render-shape-failed",tag:node.tag})}hasBorder&&!uniformBorder&&!rotate&&!borderTriangle&&shapeName!=="ellipse"&&renderBorders(slide,c,borders,slideRect,style.opacity,totals)}function isTinyRotatedBorderOnlyPseudo(node,c,hasFill,hasBorder,rotate){return node.tag==="pseudo"&&!node.text&&!hasFill&&hasBorder&&rotate&&Math.max(c.w,c.h)<.35&&Math.min(c.w,c.h)<.18}function shouldSkipNativeDecorativeBlurBox(node,c,style){if(!String(style?.filter||"").includes("blur(")||nodeHasTextDescendant(node)||(node.children||[]).some(child=>child.tag&&child.tag!=="#text")||c.w*c.h/(PPT_W*PPT_H)<.08)return!1;let backgroundImage=String(style.backgroundImage||"");return!!(!transparentCssPaint(style.backgroundColor)||backgroundImage&&backgroundImage!=="none"||style.boxShadow&&style.boxShadow!=="none")}function nodeHasTextDescendant(node){return node.tag==="#text"&&normalizeText(node.text||"")||node.text&&normalizeText(node.text)?!0:(node.children||[]).some(child=>nodeHasTextDescendant(child))}function renderBorders(slide,c,borders,slideRect,opacity,totals){let[top,right,bottom,left]=borders,borderRects=[top.width>0&&top.color?{x:c.x,y:c.y,w:c.w,h:Math.max(.002,top.width/slideRect.h*PPT_H),color:top.color,alpha:top.alpha}:null,right.width>0&&right.color?{x:c.x+c.w-Math.max(.002,right.width/slideRect.w*PPT_W),y:c.y,w:Math.max(.002,right.width/slideRect.w*PPT_W),h:c.h,color:right.color,alpha:right.alpha}:null,bottom.width>0&&bottom.color?{x:c.x,y:c.y+c.h-Math.max(.002,bottom.width/slideRect.h*PPT_H),w:c.w,h:Math.max(.002,bottom.width/slideRect.h*PPT_H),color:bottom.color,alpha:bottom.alpha}:null,left.width>0&&left.color?{x:c.x,y:c.y,w:Math.max(.002,left.width/slideRect.w*PPT_W),h:c.h,color:left.color,alpha:left.alpha}:null].filter(Boolean);for(let rect of borderRects)slide.addShape("rect",{x:rect.x,y:rect.y,w:rect.w,h:rect.h,fill:{color:rect.color,transparency:combinedTransparency(rect.alpha,opacity)},line:{color:rect.color,transparency:100}}),totals.shapeObjects+=1}function uniformBorderStyle(borders){if(!borders.length||!borders.every(border=>border.width>0&&border.color))return null;let[first]=borders;return borders.every(border=>Math.abs(border.width-first.width)<.25&&border.color===first.color&&Math.abs(border.alpha-first.alpha)<.02)?first:null}function renderText(slide,node,slideRect,warnings,totals){let value=applyTextTransform(node.text||"",node.style?.textTransform);if(!value.trim())return;let c=coords(node,slideRect);if(c.w<.01||c.h<.01)return;let style=node.style||{},color=textColorForStyle(style,node),fontSizePx=Math.max(4,Math.min(900,parseFloat(style.fontSize||"16")||16));if(isDecorativeStrokeOnlyText(style,fontSizePx)||isDecorativeLowAlphaText(color,style,fontSizePx)||isDecorativeRotatedSmallText(value,style,fontSizePx,node)||isDecorativeSparkleText(value))return;let fontFace=fontFaceForText(style.fontFamily,value),weight=String(style.fontWeight||""),singleLine=node.singleLine&&!/[\r\n]/.test(value),verticalText=isVerticalWritingMode(style),verticalContainerText=!!node.textMetrics?.verticalContainer,autoWidth=!node.clipped&&!verticalText&&singleLine&&shouldUseAutoWidthText(value,fontSizePx,c,node),clippedLine=!verticalText&&singleLine?clippedSingleLineBox(node,slideRect,c):null,align=normalizeAlign(style.textAlign),options={x:c.x,y:c.y,h:Math.max(.04,c.h+.03),margin:0,breakLine:!1,fit:autoWidth?"resize":"shrink",wrap:autoWidth?!1:!isNoWrap(style.whiteSpace),fontFace,fontSize:pptFontSize(fontSizePx,fontFace,style,value),color:color.color,bold:weight==="bold"||Number.parseInt(weight,10)>=600,italic:style.fontStyle==="italic",underline:String(style.textDecorationLine||"").includes("underline"),strike:String(style.textDecorationLine||"").includes("line-through"),align,valign:verticalContainerText?"mid":normalizeValign(style.verticalAlign),rotate:rotateFromTransform(style.transform)||0,transparency:combinedTransparency(color.alpha,style.opacity),charSpacing:letterSpacing(style.letterSpacing)},yOffset=verticalContainerText?0:pptTextYOffset(c,fontSizePx,fontFace,style,value,node);yOffset&&(options.y+=yOffset);let lineSpacing=pptLineSpacing(style.lineHeight,fontSizePx,fontFace,style,value);if(lineSpacing&&(options.lineSpacing=lineSpacing),verticalText&&(options.vert="eaVert"),/Songti SC/i.test(fontStack(style,fontFace))&&fontSizePx>=80&&node.parentTag==="span"&&(options.y=Math.max(0,options.y-c.h*.28)),style.materialBackground==="true"&&fontSizePx>=72&&/PingFang SC|Songti SC/i.test(fontStack(style,fontFace))&&(options.y+=c.h*.12),clippedLine){options.wrap=!1,options.fit="none",options.x=clippedLine.x,options.w=Math.max(.08,clippedLine.w+.04);let fitted=fittedSingleLineFontPt(value,clippedLine.w,fontFace,style);fitted&&fitted<options.fontSize&&(options.fontSize=fitted);let lineHeightPx=Number.parseFloat(style.lineHeight),stageScale=slideRect.h/SOURCE_H;if(Number.isFinite(lineHeightPx)&&lineHeightPx>0&&node.lineRect.h>lineHeightPx*stageScale){let cssLineTop=node.lineRect.y+(node.lineRect.h-lineHeightPx*stageScale)/2;options.y=round((cssLineTop-slideRect.y)/slideRect.h*PPT_H)}}else autoWidth?options.w=singleLineWidth(value,fontSizePx,c,fontFace,style):options.w=Math.max(.08,c.w+.04);autoWidth&&align!=="left"&&(align==="right"&&(options.x=Math.max(0,c.x+c.w-options.w)),align==="center"&&(options.x=Math.max(0,c.x+c.w/2-options.w/2)));try{slide.addText(value,options),totals.textObjects+=1}catch{warnings.push({slide:node.slideIndex,type:"render-text-failed",text:value.slice(0,60)})}}function isDecorativeStrokeOnlyText(style,fontSizePx){if((parseFloat(style?.webkitTextStrokeWidth||"0")||0)<=0||fontSizePx<120)return!1;let fill=parseCssColor(style?.webkitTextFillColor),color=parseCssColor(style?.color),stroke=parseCssColor(style?.webkitTextStrokeColor);return!fill&&!color&&(!stroke||stroke.alpha<=.25)}function isDecorativeLowAlphaText(color,style,fontSizePx){let opacity=Number(style?.opacity||1),alpha=Math.max(0,Math.min(1,Number(color?.alpha??1)*(Number.isFinite(opacity)?opacity:1)));return fontSizePx>=100&&alpha<=.08}function isDecorativeRotatedSmallText(value,style,fontSizePx,node={}){if(node.source==="svg-text"||node.requiredText||isVerticalWritingMode(style)||!rotateFromTransform(style?.transform)||fontSizePx>32)return!1;let color=parseCssColor(style?.webkitTextFillColor)||parseCssColor(style?.color),opacity=Number(style?.opacity||1);return Math.max(0,Math.min(1,Number(color?.alpha??1)*(Number.isFinite(opacity)?opacity:1)))<=.18&&String(value||"").trim().length>=4}function isVerticalWritingMode(style={}){return String(style.writingMode||"").includes("vertical")}function isDecorativeSparkleText(value){return/^[✦✧✶✷✸✹✺✻✼✽✾✿★☆＊*]+$/.test(String(value||"").trim())}function shouldUseAutoWidthText(value,fontSizePx,box,node){let text=String(value||"").trim();if(!text)return!1;let units=textUnits(text),parentTag=String(node.parentTag||"");return["p","li","td","th","blockquote"].includes(parentTag)&&units>24?!1:units<=20||fontSizePx>=36&&units<=32||!["p","li","td","th","blockquote"].includes(parentTag)&&fontSizePx>=24&&units<=44?!0:box.w<1.3&&units<=28}function singleLineWidth(value,fontSizePx,box,fontFace,style={}){let fontPt=pptFontSize(fontSizePx,fontFace,style,value),units=textUnits(value),spacing=Math.max(0,letterSpacing(style.letterSpacing))*Math.max(0,Array.from(String(value||"")).length-1)/72*1.75,estimated=units*fontPt/72+spacing,width=Math.max(.08,box.w+.1,estimated+.12);return Math.min(PPT_W-Math.max(0,box.x),width)}function clippedSingleLineBox(node,slideRect,c){if(!node?.clipped||!node.lineRect)return null;let x=round((node.lineRect.x-slideRect.x)/slideRect.w*PPT_W),w=round(node.lineRect.w/slideRect.w*PPT_W);return!Number.isFinite(x)||!Number.isFinite(w)||w<=c.w+.02?null:{x,w}}function fittedSingleLineFontPt(value,lineWidthIn,fontFace,style={}){let chars=Array.from(String(value||"")).length;if(!chars||!(lineWidthIn>0))return 0;let widthPt=lineWidthIn*72,stack=fontStack(style,fontFace);if(/Space Mono|IBM Plex Mono|JetBrains Mono|SFMono|ui-monospace|monospace|Menlo|Consolas|Courier/i.test(stack)&&!hasCjkText(value)){let spacing=letterSpacing(style.letterSpacing)*Math.max(0,chars-1);return Math.max(4,(widthPt-spacing)/(.6*chars))}let units=textUnits(value);return units?Math.max(4,widthPt/units):0}function textUnits(value){let units=0;for(let char of String(value||""))/\s/.test(char)?units+=.32:/[\u2e80-\u9fff]/.test(char)?units+=.96:/[A-Z0-9]/.test(char)?units+=.64:units+=.55;return units}function renderNodeImage(slide,node,slideRect,warnings,totals){let items=[];if(node.patternImageData&&items.push({data:node.patternImageData,kind:"pattern-background"}),node.backgroundImageData&&items.push({data:node.backgroundImageData,kind:"background-image",transparency:node.backgroundImageTransparency}),node.imageData&&items.push({data:node.imageData,kind:node.imageKind||node.tag}),!items.length)return;let c=coords(node,slideRect,{visual:node.elementScreenshot}),rotate=node.elementScreenshot?0:rotateFromTransform(node.style?.transform)||0;for(let item of items)try{slide.addImage({data:adaptTransparentPng(item.data,{trimLowAlphaEdges:!shouldPreserveTransparentEdges(node,item.kind)}),x:c.x,y:c.y,w:c.w,h:c.h,transparency:item.transparency??elementTransparency(node.style?.opacity),sizing:imageSizing(node,c,item.kind),rotate:rotate||void 0,shadow:localBackgroundShadow(node.style,item.kind)||void 0}),totals.imageObjects+=1}catch{warnings.push({slide:node.slideIndex,type:"render-image-failed",tag:node.tag,kind:item.kind})}}function isBrowserVisualImageKind(kind){return["material-background","unicorn-background","effect-background","masked-element","blend-group"].includes(kind)}function shouldPreserveTransparentEdges(node,kind){return node?.elementScreenshot&&isBrowserVisualImageKind(kind)}function localBackgroundShadow(style,kind){return kind!=="background-image"&&kind!=="pattern-background"&&kind!=="material-background"||kind==="material-background"?null:parseBoxShadow(style?.boxShadow)||parseDropShadow(style?.filter)}function coords(node,slideRect,options={}){let rect=options.visual?node.rect:node.renderRect||node.rect;return{x:round((rect.x-slideRect.x)/slideRect.w*PPT_W),y:round((rect.y-slideRect.y)/slideRect.h*PPT_H),w:round(rect.w/slideRect.w*PPT_W),h:round(rect.h/slideRect.h*PPT_H)}}function imageSizing(node,c,kind){let style=node.style||{},fit=style.objectFit||(style.backgroundSize==="cover"||style.backgroundSize==="contain"?style.backgroundSize:"");if((kind==="background-image"||node.tag==="img")&&(fit==="cover"||fit==="contain"))return{type:fit,w:c.w,h:c.h}}function shouldSuppressGradientFillApproximation(node,style,c){let background=String(style.backgroundImage||"");return!background.includes("radial-gradient")||!backgroundHasTransparentStop(background)||(c.w||0)*(c.h||0)/Math.max(1e-4,PPT_W*PPT_H)<=.12?!1:!(node.children||[]).length}function cssBorderTriangle(style={},c){let sides=[{side:"top",width:parseFloat(style.borderTopWidth||"0")||0,color:parseCssColor(style.borderTopColor)},{side:"right",width:parseFloat(style.borderRightWidth||"0")||0,color:parseCssColor(style.borderRightColor)},{side:"bottom",width:parseFloat(style.borderBottomWidth||"0")||0,color:parseCssColor(style.borderBottomColor)},{side:"left",width:parseFloat(style.borderLeftWidth||"0")||0,color:parseCssColor(style.borderLeftColor)}],visible=sides.filter(item=>item.width>0&&item.color),transparent=sides.filter(item=>item.width>0&&!item.color);if(visible.length!==1||transparent.length<2)return null;let color=visible[0].color,pointsBySide={top:[{x:0,y:0,moveTo:!0},{x:round(c.w),y:0},{x:round(c.w/2),y:round(c.h)},{close:!0}],right:[{x:round(c.w),y:0,moveTo:!0},{x:round(c.w),y:round(c.h)},{x:0,y:round(c.h/2)},{close:!0}],bottom:[{x:round(c.w/2),y:0,moveTo:!0},{x:round(c.w),y:round(c.h)},{x:0,y:round(c.h)},{close:!0}],left:[{x:0,y:0,moveTo:!0},{x:round(c.w),y:round(c.h/2)},{x:0,y:round(c.h)},{close:!0}]};return{fill:color,points:pointsBySide[visible[0].side]}}function cssClipPolygonPoints(clipPath,c){let match=String(clipPath||"").trim().match(/^polygon\((.*)\)$/i);if(!match)return null;let points=splitCssArgs(match[1]).map(part=>{let coords2=String(part).trim().split(/\s+/).filter(Boolean);if(coords2.length<2)return null;let x=cssClipCoord(coords2[0],c.w),y=cssClipCoord(coords2[1],c.h);return!Number.isFinite(x)||!Number.isFinite(y)?null:{x:round(x),y:round(y)}});return points.length<3||points.some(point=>!point)?null:points.map((point,index)=>index===0?{...point,moveTo:!0}:point).concat({close:!0})}function cssClipCoord(value,size){let raw=String(value||"").trim();if(raw.endsWith("%"))return Number.parseFloat(raw)/100*size;let n=Number.parseFloat(raw);return Number.isFinite(n)?n/96:NaN}function textColorForStyle(style,node={}){let fill=parseCssColor(style?.webkitTextFillColor);if(fill)return fill;if(isTextClippedBackground(style)){let gradientColor=colorFromBackgroundImage(style?.backgroundImage);if(gradientColor)return gradientColor}let stroke=(parseFloat(style?.webkitTextStrokeWidth||"0")||0)>0?parseCssColor(style?.webkitTextStrokeColor):null;if(stroke)return{...stroke,alpha:Math.min(stroke.alpha,.22)};let svgFill=parseCssColor(style?.fill);if(node.source==="svg-text"&&svgFill)return svgFill;let color=parseCssColor(style?.color);return color||svgFill||{color:"111111",alpha:1}}function parseCssColor(value){let raw=String(value||"").trim();if(!raw||raw==="transparent")return null;if(/^#([0-9a-f]{3}|[0-9a-f]{6})$/i.test(raw)){let hex=raw.slice(1);return{color:(hex.length===3?hex.replace(/./g,c=>c+c):hex).toUpperCase(),alpha:1}}if(!/^(?:rgba?|color)\(/i.test(raw))return null;let parsed=parseCanvasColor(raw);return parsed.a<=.01?null:canvasColorToCss(parsed)}function colorFromBackgroundImage(value){let raw=String(value||"");if(!raw.includes("gradient"))return null;let colors=[...raw.matchAll(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/ig)].map(match=>parseCssColor(match[0])).filter(Boolean);if(!colors.length)return null;let baseColors=colors.filter(color=>color.alpha>=.85),source=baseColors.length?baseColors:colors,rgb=source.reduce((acc,color)=>(acc.r+=Number.parseInt(color.color.slice(0,2),16),acc.g+=Number.parseInt(color.color.slice(2,4),16),acc.b+=Number.parseInt(color.color.slice(4,6),16),acc.a+=color.alpha,acc),{r:0,g:0,b:0,a:0}),count=source.length;return{color:[rgb.r/count,rgb.g/count,rgb.b/count].map(n=>clampColor(n).toString(16).padStart(2,"0")).join("").toUpperCase(),alpha:Math.max(0,Math.min(1,rgb.a/count)),gradient:!0}}function canvasColorToCss(color){return{color:[color.r,color.g,color.b].map(n=>Math.round(Math.max(0,Math.min(255,n))).toString(16).padStart(2,"0")).join("").toUpperCase(),alpha:Math.max(0,Math.min(1,Number(color.a??1)))}}function readBorders(style){return["Top","Right","Bottom","Left"].map(side=>{let color=parseCssColor(style[`border${side}Color`]),width=parseFloat(style[`border${side}Width`]||"0")||0,styleValue=style[`border${side}Style`];return{width:styleValue==="none"||styleValue==="hidden"?0:width,color:color?.color||null,alpha:color?.alpha||0}})}function isCircleLikeBox(node={},radiusPx=0){let rect=node.rect||{},width=Number(rect.w||0),height=Number(rect.h||0),minSide=Math.min(width,height),maxSide=Math.max(width,height);return!minSide||maxSide/minSide>1.18?!1:radiusPx>=minSide*.42}function parseBoxShadow(value){let raw=String(value||"");if(!raw||raw==="none")return null;let color=parseCssColor(raw.match(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/i)?.[0]);if(!color)return null;let numbers=raw.replace(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/ig,"").match(/-?\d+(\.\d+)?px/g)||[],offsetX=parseFloat(numbers[0]||"0")||0,offsetY=parseFloat(numbers[1]||"0")||0,blur=parseFloat(numbers[2]||"8")||8;return pptShadow(color,offsetX,offsetY,blur)}function parseDropShadow(value){let match=String(value||"").match(/drop-shadow\(([^)]+(?:\)[^)]+)?)\)/i);if(!match)return null;let body=match[1],color=parseCssColor(body.match(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/i)?.[0])||{color:"000000",alpha:.35},numbers=body.replace(/rgba?\([^)]+\)|color\([^)]+\)|#[0-9a-f]{3,8}/ig,"").match(/-?\d+(\.\d+)?px/g)||[],offsetX=parseFloat(numbers[0]||"0")||0,offsetY=parseFloat(numbers[1]||"0")||0,blur=parseFloat(numbers[2]||"8")||8;return pptShadow(color,offsetX,offsetY,blur)}function pptShadow(color,offsetX,offsetY,blur){let angle=(Math.atan2(offsetY,offsetX)*180/Math.PI+360)%360,offset=Math.sqrt(offsetX**2+offsetY**2)*PX_TO_PT;return{type:"outer",color:color.color,opacity:Math.max(.05,Math.min(.7,color.alpha)),blur:Math.max(1,Math.min(24,blur*PX_TO_PT)),offset:Math.max(1,Math.min(18,offset)),angle}}function combinedTransparency(alpha,opacity){let composite=Math.max(0,Math.min(1,Number(alpha??1)*Number(opacity||1)));return Math.round((1-composite)*100)}function elementTransparency(opacity){return combinedTransparency(1,opacity)}function letterSpacing(value){let n=parseFloat(value||"0");return Number.isFinite(n)?Math.max(-2,Math.min(12,n*PX_TO_PT)):0}function fontFamilies(value){return String(value||"Arial").split(",").map(item=>item.trim().replace(/^["']|["']$/g,"")).filter(Boolean)}function fontFaceForText(fontFamily,text=""){let families=fontFamilies(fontFamily);if(hasCjkText(text)){let cjk=families.find(isCjkFontFamily);if(cjk)return pptxSafeCjkFontFace(cjk)}return pptxSafeFontFace(families[0]||"Arial")}function pptxSafeCjkFontFace(family){return/Songti|宋体|Serif SC|Source Han Serif/i.test(String(family||""))?"SimSun":"Microsoft YaHei"}function pptxSafeFontFace(family){let name=String(family||"").trim();for(let[pattern,safe]of PPTX_SAFE_FONT_MAP)if(pattern.test(name))return safe;return name||"Arial"}function isCjkFontFamily(value){return/Noto Sans SC|PingFang SC|Songti SC|Microsoft YaHei|Source Han|思源|黑体|宋体/i.test(String(value||""))}function fontStack(style={},fontFace=""){return[fontFace,style.fontFamily].filter(Boolean).join(",")}function pptFontSize(px,fontFace,style={},text=""){return px*pptFontScale(fontFace,style,text)}function pptFontScale(fontFace,style={},text=""){let stack=fontStack(style,fontFace);return/Space Mono|IBM Plex Mono|SFMono|ui-monospace|monospace|Menlo/i.test(stack)?.66:hasCjkText(text)||/Noto Sans SC|PingFang SC|Songti SC|Microsoft YaHei|Source Han|思源|黑体|宋体|sans-serif|system-ui|-apple-system/i.test(stack)?.6:PX_TO_PT}function hasCjkText(value){return/[\u2e80-\u9fff]/.test(String(value||""))}function pptLineSpacing(value,fontSizePx,fontFace,style={},text=""){let raw=String(value||"").trim();if(!raw||raw==="normal")return null;let n=parseFloat(raw);if(!Number.isFinite(n)||n<=0)return null;let lineHeightPx=raw.endsWith("px")?n:n<=4?n*fontSizePx:n;return!Number.isFinite(lineHeightPx)||lineHeightPx<=0?null:pptFontSize(lineHeightPx,fontFace,style,text)}function pptTextYOffset(box,fontSizePx,fontFace,style={},text="",node=null){let stack=fontStack(style,fontFace);if(fontSizePx<28)return 0;let parentTag=String(node?.parentTag||""),cjkStack=/Noto Sans SC|PingFang SC|Songti SC|Microsoft YaHei|Source Han|思源|黑体|宋体|sans-serif|system-ui|-apple-system/i.test(stack),numericText=/^[\s¥$€£+\-−–—.,:%/0-9A-Za-z]+$/.test(String(text||""));if(numericText&&cjkStack){if(fontSizePx>=220)return box.h*.12;if(fontSizePx>=160)return box.h*.1;if(fontSizePx>=96)return box.h*.07;if(fontSizePx>=60)return box.h*.045}let monoStack=/Space Mono|IBM Plex Mono|JetBrains Mono|SFMono|ui-monospace|monospace|Menlo|Consolas|Courier/i.test(stack);if(numericText&&monoStack){if(fontSizePx>=220)return box.h*.12;if(fontSizePx>=160)return box.h*.1;if(fontSizePx>=96)return box.h*.07;if(fontSizePx>=60)return box.h*.045}return parentTag==="span"&&/^[\s¥$€£+\-−–—.,:%/0-9万亿兆]+$/.test(String(text||""))&&cjkStack&&fontSizePx>=72?-box.h*.035:hasCjkText(text)?fontSizePx>=180?box.h*.05:fontSizePx>=120?box.h*.04:fontSizePx>=96?box.h*.03:fontSizePx>=48?box.h*.018:box.h*.006:/Anton/i.test(stack)&&fontSizePx>=80?box.h*.09:/Space Grotesk|Archivo|Arimo|IBM Plex Sans|Newsreader|Caveat/i.test(stack)&&fontSizePx>=48?box.h*.045:0}function normalizeAlign(value){return value==="center"||value==="right"||value==="justify"?value:"left"}function normalizeValign(value){return value==="bottom"||value==="sub"?"bottom":value==="middle"?"mid":"top"}function isNoWrap(value){return value==="nowrap"}function applyTextTransform(text,transform){return transform==="uppercase"?text.toUpperCase():transform==="lowercase"?text.toLowerCase():text}function clampColor(value){return Math.max(0,Math.min(255,Math.round(Number.parseFloat(value)||0)))}function round(value){return Math.round(value*1e4)/1e4}async function buildEditablePptx(PptxGenJSCtor,deck,options={}){let title=options.title||"Editable Deck Export",pptx=new PptxGenJSCtor;pptx.defineLayout({name:"DASHI_WIDE",width:PPT_W,height:PPT_H}),pptx.layout="DASHI_WIDE",pptx.author="Dashi PPT",pptx.subject="Editable PPTX export",pptx.title=title;let warnings=[...deck.warnings],totals={textObjects:0,shapeObjects:0,imageObjects:0},slideSummaries=[];for(let slideIndex=0;slideIndex<deck.slides.length;slideIndex+=1){let slideData=deck.slides[slideIndex];await emitProgress(options.onProgress,{stage:"rendering",detail:`\u751F\u6210 PPTX \u5BF9\u8C61 ${slideIndex+1}/${deck.slides.length}`,percent:68+Math.round(slideIndex/Math.max(1,deck.slides.length)*20)});let slide=pptx.addSlide();slide.background={color:"FFFFFF"};let before={...totals};renderCapturedNode(slide,slideData.root,slideData.rect,warnings,totals),warnings.push(...slideData.warnings),slideSummaries.push({index:slideData.index,key:slideData.summary?.key||"",capturedNodes:slideData.summary?.capturedNodes||0,maxDepth:slideData.summary?.maxDepth||0,textNodes:slideData.summary?.textNodes||0,backgroundImages:slideData.summary?.backgroundImages||0,svgImages:slideData.summary?.svgImages||0,canvasImages:slideData.summary?.canvasImages||0,imageNodes:slideData.summary?.imageNodes||0,shapeCandidates:slideData.summary?.shapeCandidates||0,renderedTextObjects:totals.textObjects-before.textObjects,renderedShapeObjects:totals.shapeObjects-before.shapeObjects,renderedImageObjects:totals.imageObjects-before.imageObjects})}return{pptx,warnings,totals,slideSummaries}}var __pptxFontTestables={pptxSafeFontFace};var require2=createRequire(import.meta.url),PptxGenJS=require2("pptxgenjs");async function exportEditablePptxFromPage(page,options={}){let outFile=path.resolve(options.outFile||"editable-export.pptx"),reportFile=options.reportFile?path.resolve(options.reportFile):null,title=options.title||"Editable Deck Export";await emitProgress(options.onProgress,{stage:"collecting",detail:"\u91C7\u96C6\u9875\u9762\u7ED3\u6784",percent:14});let deck=await collectEditableDeck(page,options),{pptx,warnings,totals,slideSummaries}=await buildEditablePptx(PptxGenJS,deck,{title,onProgress:options.onProgress});mkdirSync(path.dirname(outFile),{recursive:!0}),await emitProgress(options.onProgress,{stage:"saving",detail:"\u4FDD\u5B58 PPTX \u6587\u4EF6",percent:92}),await pptx.writeFile({fileName:outFile});let report={captureMode:"captured-tree",slideCount:deck.slides.length,textObjects:totals.textObjects,shapeObjects:totals.shapeObjects,imageObjects:totals.imageObjects,slideSummaries,warnings};return reportFile&&(mkdirSync(path.dirname(reportFile),{recursive:!0}),writeFileSync(reportFile,JSON.stringify(report,null,2)+`
`)),await emitProgress(options.onProgress,{stage:"ready",detail:"\u51C6\u5907\u4E0B\u8F7D\u6587\u4EF6",percent:98}),{outFile,reportFile,...report}}async function exportEditablePptxFromUrl(browser,url,options={}){let context=await browser.newContext({viewport:{width:SOURCE_W,height:SOURCE_H},ignoreHTTPSErrors:!0}),page=await context.newPage();try{return page.setDefaultTimeout(options.timeout||45e3),await emitProgress(options.onProgress,{stage:"opening",detail:"\u6253\u5F00\u9884\u89C8\u9875\u9762",percent:8}),await page.goto(url,{waitUntil:"domcontentloaded"}),await page.waitForSelector(options.activeSlideSelector||"#deck > .slide.active, #deck > .slide[data-deck-active]"),await emitProgress(options.onProgress,{stage:"preparing",detail:"\u51C6\u5907\u5BFC\u51FA\u9875\u9762\u72B6\u6001",percent:12}),options.snapshot&&await applyDeckSnapshot(page,options.snapshot),await exportEditablePptxFromPage(page,options)}finally{await page.close().catch(()=>{}),await context.close().catch(()=>{})}}async function applyDeckSnapshot(page,snapshot){await page.evaluate(applyEditablePptxSnapshot,snapshot)}async function collectEditableDeck(page,options={}){await page.evaluate(async({includeAllThemePacks})=>{if(window.__editablePptxRestoreState={locked:window.__deckExportLocked,themePack:document.documentElement.dataset.themePack||""},includeAllThemePacks&&window.__setActiveThemePack?.("",{navigate:!1}),window.__deckExportLocked=!0,!document.getElementById("dashi-export-no-radius")){let style=document.createElement("style");style.id="dashi-export-no-radius",style.textContent="#deck-viewport,#deck,#deck>.slide{border-radius:0!important}",document.head.appendChild(style)}window.__flushEditableTextState?.(),window.__syncDeckViewModelFromDom?.(),window.__setEditableTextMode?.(!1),window.__layoutDeck?.(),await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)))},{includeAllThemePacks:options.includeAllThemePacks===!0});try{await installBrowserCollector(page);let slides=[],warnings=[],variantOutputMode=(options.snapshot?.variantOutputMode??options.variantOutputMode)==="selected-only"?"selected-only":"comparison",descriptors=await page.evaluate(getExportSlideDescriptors,variantOutputMode);if(Array.isArray(options.slideIndexes)&&options.slideIndexes.length){let requestedIndexes=new Set(options.slideIndexes.map(index=>Number(index)).filter(index=>Number.isInteger(index)&&index>=0));descriptors=descriptors.filter(descriptor=>requestedIndexes.has(descriptor.logicalIndex))}let count=descriptors.length;for(let exportIndex=0;exportIndex<count;exportIndex+=1){let descriptor=descriptors[exportIndex],restoreToken=null;try{restoreToken=await page.evaluate(materializeExportSlideDescriptor,descriptor),await emitProgress(options.onProgress,{stage:"collecting",detail:`\u91C7\u96C6\u9875\u9762\u7ED3\u6784 ${exportIndex+1}/${count}`,percent:16+Math.round(exportIndex/Math.max(1,count)*48)}),await page.evaluate(async index=>{window.go?.(index,{animate:!1,force:!0});let slides2=window.__getVisibleSlides?.()||[...document.querySelectorAll("#deck > .slide:not([hidden])")];window.__ensureRuntimeSlideRendered?.(slides2[index]),window.__applyEditablePptxSnapshotText?.(slides2[index]),window.__restoreEffectIframes?.(slides2[index]),window.__layoutDeck?.(),await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))),window.__finishEditablePptxAnimations?.(slides2[index]||document),await new Promise(resolve=>requestAnimationFrame(resolve)),await new Promise(resolve=>setTimeout(resolve,120))},descriptor.logicalIndex);let slideData=await page.evaluate(index=>window.__collectEditablePptxSlide(index),exportIndex+1);await resolveElementScreenshots(page,slideData.root,warnings,{freeze:options.freezeElementScreenshots===!0}),slides.push(slideData)}finally{restoreToken&&await page.evaluate(restoreExportSlideDescriptor,restoreToken)}}return{slides,warnings}}finally{await page.evaluate(async()=>{let restore=window.__editablePptxRestoreState||{};(document.documentElement.dataset.themePack||"")!==(restore.themePack||"")&&window.__setActiveThemePack?.(restore.themePack||"",{navigate:!1}),window.__deckExportLocked=!!restore.locked,window.__setEditableTextMode?.(window.__canEditDeck?.()),delete window.__editablePptxSnapshotTextState,delete window.__applyEditablePptxSnapshotText,delete window.__editablePptxRestoreState,window.__layoutDeck?.(),await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)))}).catch(()=>{})}}async function resolveElementScreenshots(page,root,warnings,options={}){let targets=[];walkCapturedNodes(root,node=>{node.elementScreenshot&&node.exportId&&targets.push(node)});for(let node of targets){let hiddenToken=null;try{(node.stripTextForScreenshot||node.stripOverlayForScreenshot)&&(hiddenToken=await page.evaluate(hideNodeForScreenshot,{exportId:node.exportId,mode:node.screenshotMode||(node.imageKind==="unicorn-background"?"screenshot-rect":"descendant"),stripText:!!node.stripTextForScreenshot,stripOverlay:!!node.stripOverlayForScreenshot,screenshotRect:node.screenshotRect||null})),hiddenToken&&await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));let locator=page.locator(`#deck > .slide.active [data-editable-pptx-export-id="${node.exportId}"], #deck > .slide[data-deck-active] [data-editable-pptx-export-id="${node.exportId}"]`).first(),clip=node.screenshotRect?screenshotClip(node.screenshotRect):null,bytes=null;if(shouldUseAlphaMatteScreenshot(node)&&(bytes=await captureAlphaMatteScreenshot(page,locator,node.exportId,node.imageKind,clip).catch(()=>null),bytes||warnings.push({slide:node.slideIndex,type:"alpha-matte-screenshot-failed",tag:node.tag,kind:node.imageKind})),bytes||(bytes=clip?await page.screenshot({type:"png",clip}):await locator.screenshot({type:"png"})),node.elementScreenshot&&shouldApplyNodeRadiusAlphaMask(node)&&(bytes=applyNodeRadiusAlphaMask(bytes,node)),node.imageData=pngBufferToDataUrl(bytes),hiddenToken&&(await page.evaluate(restoreHiddenScreenshotStyles,hiddenToken).catch(()=>{}),hiddenToken=null),!options.freeze)continue;await page.evaluate(({exportId,data})=>{let el=document.querySelector(`#deck > .slide.active [data-editable-pptx-export-id="${exportId}"], #deck > .slide[data-deck-active] [data-editable-pptx-export-id="${exportId}"]`)||document.querySelector(`[data-editable-pptx-export-id="${exportId}"]`);if(!el)return;el.replaceChildren();let img=document.createElement("img");img.src=data,img.setAttribute("data-editable-pptx-frozen-layer",""),Object.assign(img.style,{position:"absolute",inset:"0",width:"100%",height:"100%",objectFit:"cover",pointerEvents:"none"}),el.appendChild(img)},{exportId:node.exportId,data:node.imageData})}catch{warnings.push({slide:node.slideIndex,type:"element-screenshot-failed",tag:node.tag,kind:node.imageKind})}finally{hiddenToken&&await page.evaluate(restoreHiddenScreenshotStyles,hiddenToken).catch(()=>{})}}}async function captureAlphaMatteScreenshot(page,locator,exportId,imageKind,clip=null){let token=null;try{if(token=await page.evaluate(({exportId:exportId2,imageKind:imageKind2})=>{let root=document.querySelector(`#deck > .slide.active [data-editable-pptx-export-id="${exportId2}"], #deck > .slide[data-deck-active] [data-editable-pptx-export-id="${exportId2}"]`)||document.querySelector(`[data-editable-pptx-export-id="${exportId2}"]`);if(!root)return null;let slide=root.closest("#deck > .slide")||document.querySelector("#deck > .slide.active, #deck > .slide[data-deck-active]");if(!slide)return null;let token2=`alpha-matte-${Date.now()}-${Math.random().toString(36).slice(2)}`,entries=[],matteEls=[document.documentElement,document.body,slide].filter(Boolean),remember=el=>{!el||entries.some(entry=>entry.el===el)||entries.push({el,style:el.getAttribute("style")})},setStyle=(el,name,value)=>{remember(el),el.style.setProperty(name,value,"important")},neutralizeAncestor=el=>{setStyle(el,"background-image","none"),setStyle(el,"background-color","transparent"),setStyle(el,"box-shadow","none"),setStyle(el,"border-color","transparent")};neutralizeAncestor(document.documentElement),neutralizeAncestor(document.body),[...slide.querySelectorAll("*")].forEach(el=>{el===root||root.contains(el)||(el.contains(root)?neutralizeAncestor(el):setStyle(el,"opacity","0"))}),setStyle(slide,"background-image","none"),setStyle(slide,"background-color","#000"),setStyle(slide,"box-shadow","none"),setStyle(root,"mix-blend-mode","normal"),imageKind2==="unicorn-background"&&(setStyle(root,"background-image","none"),setStyle(root,"background-color","transparent"));for(let el of matteEls)setStyle(el,"background-color","#000");return window.__editablePptxAlphaMatteStyles||=new Map,window.__editablePptxAlphaMatteStyles.set(token2,{entries,matteEls}),token2},{exportId,imageKind}),!token)return null;await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));let shoot=()=>clip?page.screenshot({type:"png",clip}):locator.screenshot({type:"png"}),blackBytes=await shoot();await page.evaluate(({token:token2,color})=>{let state=window.__editablePptxAlphaMatteStyles?.get(token2);if(state)for(let el of state.matteEls||[])el.style.setProperty("background-color",color,"important")},{token,color:"#fff"}),await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));let whiteBytes=await shoot();return composeAlphaMattePng(blackBytes,whiteBytes)}finally{token&&await page.evaluate(token2=>{let entries=window.__editablePptxAlphaMatteStyles?.get(token2)?.entries||[];for(let entry of entries)entry.style==null?entry.el.removeAttribute("style"):entry.el.setAttribute("style",entry.style);window.__editablePptxAlphaMatteStyles?.delete(token2)},token).catch(()=>{})}}function composeAlphaMattePng(blackBytes,whiteBytes){let black=PNG.sync.read(Buffer.from(blackBytes)),white=PNG.sync.read(Buffer.from(whiteBytes));if(black.width!==white.width||black.height!==white.height)throw new Error("Alpha matte screenshot dimensions differ.");let out=new PNG({width:black.width,height:black.height});for(let i=0;i<black.data.length;i+=4){let br=black.data[i],bg=black.data[i+1],bb=black.data[i+2],wr=white.data[i],wg=white.data[i+1],wb=white.data[i+2],delta=(wr-br+(wg-bg)+(wb-bb))/3,alpha=Math.round(255-delta);if(alpha=Math.max(0,Math.min(255,alpha)),alpha<=2){out.data[i]=0,out.data[i+1]=0,out.data[i+2]=0,out.data[i+3]=0;continue}alpha>=253&&(alpha=255);let scale=255/alpha;out.data[i]=clampByte(Math.round(br*scale)),out.data[i+1]=clampByte(Math.round(bg*scale)),out.data[i+2]=clampByte(Math.round(bb*scale)),out.data[i+3]=alpha}return bleedTransparentRgb(out),PNG.sync.write(out)}function applyNodeRadiusAlphaMask(bytes,node){let radius=maxCssRadius(node?.style||{},node?.rect?.w||0,node?.rect?.h||0);if(radius<=0)return bytes;try{let image=PNG.sync.read(Buffer.from(bytes)),scaleX=image.width/Math.max(1,Number(node?.rect?.w||image.width)),scaleY=image.height/Math.max(1,Number(node?.rect?.h||image.height)),radiusPx=Math.min(image.width/2,image.height/2,radius*Math.max(scaleX,scaleY));return radiusPx<=0?bytes:(applyRoundedAlphaMask(image,radiusPx),trimLowAlphaEdges(image),bleedTransparentRgb(image),PNG.sync.write(image))}catch{return bytes}}function applyRoundedAlphaMask(image,radius){let{width,height,data}=image,rx=Math.min(radius,width/2),ry=Math.min(radius,height/2),r=Math.min(rx,ry);for(let y=0;y<height;y+=1)for(let x=0;x<width;x+=1){let px=x+.5,py=y+.5,coverage=1,cx=px<rx?rx:px>width-rx?width-rx:px,cy=py<ry?ry:py>height-ry?height-ry:py;if(cx!==px||cy!==py){let dist=Math.hypot(px-cx,py-cy);coverage=Math.max(0,Math.min(1,r+.5-dist))}if(coverage>=1)continue;let index=(y*width+x)*4+3;data[index]=Math.round(data[index]*coverage)}}function trimLowAlphaEdges(image,threshold=18){let{width,height,data}=image,total=width*height,seen=new Uint8Array(total),queue=new Int32Array(total),head=0,tail=0,push=index=>{index<0||index>=total||seen[index]||data[index*4+3]>threshold||(seen[index]=1,queue[tail++]=index)};for(let x=0;x<width;x+=1)push(x),push((height-1)*width+x);for(let y=1;y<height-1;y+=1)push(y*width),push(y*width+width-1);for(;head<tail;){let index=queue[head++];data[index*4+3]=0;let x=index%width,y=Math.floor(index/width);x>0&&push(index-1),x<width-1&&push(index+1),y>0&&push(index-width),y<height-1&&push(index+width)}}function bleedTransparentRgb(image){let{width,height,data}=image,total=width*height,seen=new Uint8Array(total),queue=new Int32Array(total),head=0,tail=0;for(let index=0;index<total;index+=1)data[index*4+3]<=12||(seen[index]=1,queue[tail++]=index);for(;head<tail;){let index=queue[head++],x=index%width,y=Math.floor(index/width),base=index*4,neighbors=[x>0?index-1:-1,x<width-1?index+1:-1,y>0?index-width:-1,y<height-1?index+width:-1];for(let neighbor of neighbors){if(neighbor<0||seen[neighbor])continue;let target=neighbor*4;data[target]=data[base],data[target+1]=data[base+1],data[target+2]=data[base+2],seen[neighbor]=1,queue[tail++]=neighbor}}}function clampByte(value){return Math.max(0,Math.min(255,value))}function pngBufferToDataUrl(bytes){return`data:image/png;base64,${Buffer.from(bytes).toString("base64")}`}async function installBrowserCollector(page){await page.addScriptTag({content:`
      window.__collectEditablePptxSlide = (() => {
        const STYLE_KEYS = ${JSON.stringify(STYLE_KEYS)};
        ${collectActiveSlide.toString()}
        ${captureElement.toString()}
        ${capturePseudoElement.toString()}
        ${styleWithCumulativeRotation.toString()}
        ${cumulativeRotation.toString()}
        ${pseudoRect.toString()}
        ${captureWholeTextElement.toString()}
        ${isDuplicatedDecoratorTextNode.toString()}
        ${isInlineTextChild.toString()}
        ${hasInlineVisualTreatment.toString()}
        ${captureTextNode.toString()}
        ${splitWrappedTextNode.toString()}
        ${groupLineRects.toString()}
        ${lineIndexForRect.toString()}
        ${rangeBoundsForOffsets.toString()}
        ${effectiveTextStyle.toString()}
        ${elementRenderRect.toString()}
        ${visualEffectRect.toString()}
        ${svgVisualRect.toString()}
        ${expandedClampedRect.toString()}
        ${shadowOutsetPx.toString()}
        ${splitShadowLayers.toString()}
        ${visualScreenshotFallbackKind.toString()}
        ${visualScreenshotRect.toString()}
        ${hasCssMask.toString()}
        ${visibleElementChildren.toString()}
        ${shouldScreenshotBlendGroup.toString()}
        ${shouldScreenshotRoundedVisual.toString()}
        ${cornerRadiiPx.toString()}
        ${hasNonUniformCssRadius.toString()}
        ${hasRoundedClipStyle.toString()}
        ${shouldScreenshotGradientEffect.toString()}
        ${shouldUseLocalMaterialFallback.toString()}
        ${isEditableTextContainer.toString()}
        ${isInsideEditableTextContainer.toString()}
        ${hasOnlyInlineTextChildren.toString()}
        ${isInlineTextOnlyElement.toString()}
        ${shouldSkipDecorativeGradientFallback.toString()}
        ${backgroundHasTransparentStop.toString()}
        ${transparentCssPaint.toString()}
        ${hasTextPaintSource.toString()}
        ${isLowAlphaLinearGradient.toString()}
        ${readStyle.toString()}
        ${summarizeCapturedTree.toString()}
        ${summarizeNode.toString()}
        ${elementImageData.toString()}
        ${svgElementData.toString()}
        ${collectSvgTextNodes.toString()}
        ${cloneSvgWithComputedStyle.toString()}
        ${isTextClippedBackground.toString()}
        ${shouldUseNativeGradientShape.toString()}
        ${patternBackgroundImageData.toString()}
        ${parseRepeatingGradient.toString()}
        ${backgroundCropForClippedRect.toString()}
        ${gradientBackgroundImageData.toString()}
        ${drawLinearGradient.toString()}
        ${drawRadialGradient.toString()}
        ${splitCssLayers.toString()}
        ${splitCssArgs.toString()}
        ${parseGradientColorStops.toString()}
        ${parseGradientColorStop.toString()}
        ${parseCanvasColor.toString()}
        ${cssColorComponent.toString()}
        ${cssAlpha.toString()}
        ${parseGradientPosition.toString()}
        ${normalizeGradientStops.toString()}
        ${rgbaString.toString()}
        ${roundedRectPath.toString()}
        ${backgroundUrl.toString()}
        ${isTurbulenceDataImage.toString()}
        ${maxCssRadius.toString()}
        ${cssRadiusPx.toString()}
        ${slideClipRect.toString()}
        ${intersectClientRect.toString()}
        ${nextChildClipRect.toString()}
        ${hasClipStyle.toString()}
        ${sameClientRect.toString()}
        ${rotateFromTransform.toString()}
        ${scaleFromTransform.toString()}
        ${finishEditablePptxAnimations.toString()}
        ${markUnicornOverlayText.toString()}
        ${fallbackTextRisk.toString()}
        ${visibleTextInSubtree.toString()}
        ${visibleTextInScreenshotRect.toString()}
        ${visibleOverlayPaintInScreenshotRect.toString()}
        ${collectDomFallbackTextNodes.toString()}
        ${svgTextRisk.toString()}
        ${fetchImageDataUrl.toString()}
        ${normalizeDataImageUrl.toString()}
        ${rasterizeSvgDataUrl.toString()}
        ${blobToDataUrl.toString()}
        ${isVisibleElement.toString()}
        ${isMediaChrome.toString()}
        ${shouldScreenshotImageSlot.toString()}
        ${isImageSlotElement.toString()}
        ${hasTransformedAncestor.toString()}
        ${isRotatedOrSkewedTransform.toString()}
        ${clippedRect.toString()}
        ${rectObject.toString()}
        ${normalizeText.toString()}
        ${hasPaint.toString()}
        ${hasAnyBorder.toString()}
        ${cssPx.toString()}
        ${cssLengthPx.toString()}
        ${translateFromTransform.toString()}
        window.__finishEditablePptxAnimations = finishEditablePptxAnimations;
        return collectActiveSlide;
      })();
    `})}function normalizeTransparentPngDataUrl(dataUrl,options={}){let match=String(dataUrl||"").match(/^data:image\/png;base64,(.+)$/i);if(!match)return dataUrl;try{let image=PNG.sync.read(Buffer.from(match[1],"base64")),hasTransparentPixels=!1;for(let i=3;i<image.data.length;i+=4)if(image.data[i]===0){hasTransparentPixels=!0;break}return hasTransparentPixels?(options.trimLowAlphaEdges!==!1&&trimLowAlphaEdges(image),bleedTransparentRgb(image),pngBufferToDataUrl(PNG.sync.write(image))):dataUrl}catch{return dataUrl}}configureImageAdapter({normalizeTransparentPng:normalizeTransparentPngDataUrl});export{__pptxFontTestables,exportEditablePptxFromPage,exportEditablePptxFromUrl};
