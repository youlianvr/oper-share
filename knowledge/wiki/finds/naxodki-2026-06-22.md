# Findings from 22.06.2026

## omp config system
- `omp config set personality none` — kills the polite assistant
- `omp config set skills.customDirectories '["path"]'` — connects external skills
- `omp config set snapcompact.systemPrompt agents-md` — AGENTS.md as system prompt
- `systemPrompt` in config.yml — **doesn't work**, legacy field

## Portability via Symlink
- `ln -s /real/path/GroveMind ~/GroveMind` — and all paths `~/GroveMind/...` work
- opencode resolves `~` in instructions[] paths
- Moving to another PC: copy folder + symlink + config

## GroveMind Structure
- 24 files, 104 KB active system
- 0 scripts, 0 MCP, 0 CLI — only MD and injects
- Street level: simple, reliable, effective
