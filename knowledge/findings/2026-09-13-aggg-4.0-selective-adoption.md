# AGGG 4.0 (AGGG4ks-dist) — selective adoption (Freebuff + hermes only)

**Date:** 2026-09-13 · **Trigger:** owner `t.me/no_agi_chat/5590/12255` +
"только фрибаф и гермес, других харнессов у меня нету" · **Approvals:**
both MCP servers, all 14 skills into Freebuff, `camoufox-research` → trash.

## What the product is

A different line than AGGG-3.0 (kept vendored, still powering `db-tools`):
a **local MCP ecosystem** + skill farm.

- **BrowserMCP** (`mcps/BrowserMCP`): Camoufox-based "human-like" browser
  automation — 86 tools, persistent logins, TOTP 2FA, snapshots with refs,
  stealth. Playwright covers plain automation; this covers anti-detect +
  persistent identity. Smoke-tested: handshake + `tools/list` = 86 tools.
- **SearchMCP** (`mcps/SearchMCP`): one-call research (SERP fan-out → parallel
  extraction → Markdown digest), 1 tool `research`. Smoke-tested OK.
- **Skills**: 5 canon + 9 Kiro-spec, with `{{MCP:*}}` macros rendered by the
  bundled farm per harness (Freebuff/Claude prefix: `mcp__<server>__*`).

## What was done

1. **Vendored** whole dist → `projects/AGGG-4.0/` (4.1 MB, watermark kept per
   AUTHOR.md). Upstream `.gitignore` excluded `mcps/` (they are separate
   upstream git repos) — replaced with a documented deviation comment, because
   our configs point into this tree. See `projects/AGGG-4.0/PROVENANCE.md`.
2. **Farm run with env-override targeting** (blind run refused: it would have
   written symlinks into a Temp dir and resolved hermes to the stray
   `~/.hermes` instead of the real `C:\Users\pc\AppData\Local\hermes`):
   - 14 skills → Freebuff workspace as real dirs (12 byte-identical, 2
     farm-rendered, 2 EOL-only diffs);
   - 14 skill copies → real hermes `skills/`; SOUL.md untouched
     (`if_absent_only`); all other 12+ harnesses redirected to a sandbox.
3. **MCP registration** (server names must match the rendered skill prefixes
   `browsermcp` / `searchmcp` exactly): global `~/.agents/mcp.json` (32),
   workspace `.mcp.json` (29; dead `token-optimizer` entry dropped — tool was
   recycled 2026-09-12), hermes `config.yaml` (57). Launch formula:
   `uv run --project .../mcps/<Srv> -m <srv> mcp`. Backups made for all three.
4. **Trash**: `tools/camoufox-research` (dead predecessor of BrowserMCP —
   never installed anywhere; old `web-research-camoufox` skill now has a live
   server behind it).
5. **Dead-path fixes** (found during wiring): hermes `KB_DIRS` dropped
   `projects/AGGG-2.4/Wiki`; global knowledge-rag `KB_DIRS` dropped
   `projects/AGGG-3.0/Wiki`; hermes `enabled: falsetrue` corruption repaired
   (leftover of the token-optimizer teardown).
6. **Docs updated**: `docs/MCP.md` — +browsermcp/searchmcp rows (VERIFIED-LIVE
   smoke test), token-optimizer moved to disqualified with reason.

## Notes

- The farm's dry-run plan is 171 actions across 16+ harnesses — never run it
  without the env-override sandboxing trick; the dist README's install path
  assumes harnesses we do not use.
- New skills are NOT-VERIFIED in the skills catalog (installed today, zero
  usage history). Kiro suite overlaps `spec-driven-workflow` / `spec-to-repo`
  in niche only — different methodology, both stay.
- First MCP start warms uv envs (~4 s, 67/81 pkgs); later starts are fast.
- Stale `~/.hermes` (137-byte July config) left alone — noted in PROVENANCE.
